{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/script.test.ts", "before": "The line it was anchored to is gone, and a row that has forgotten where it is would insert\nsomewhere the author never pointed.", "after": "The line it was anchored to is gone, and a composer with no anchor would insert somewhere the\nauthor never pointed", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/script.test.ts", "before": "The point of the synthetic state: the *real* interaction judges a drag against it, so a\nverdict drawn during the gesture is the one `story.moveLine` would produce.", "after": "The real interaction judges a drag against this synthetic state, so a verdict drawn during the\ngesture is the one `story.moveLine` would produce", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/script.test.ts", "before": "The composition the column performs per pointer move, with the DOM read stubbed out: measured\nrows → `dropTarget` → the verdict judged on the grab. Nothing else decides anything, which is\nwhy the surface can stay thin.", "after": "The composition the column performs per pointer move, with the DOM read stubbed out: measured\nrows → `dropTarget` → the verdict judged on the grab. Nothing else decides anything, so the\nsurface stays thin", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/script.test.ts", "before": "A blank slot reads as \"there is no control here\", and the control is the whole point.", "after": "A blank slot reads as if there were no control there, so the narrator is named instead", "labels": ["P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/skills.test.ts", "before": "The whole reason `SKILLS_DIR` is spelt out rather than imported from `@vn/authoring`: every\npath this pane sees came off the wire forward-slashed, and `join()` would not match on Windows.", "after": "`SKILLS_DIR` is spelt out rather than imported from `@vn/authoring` because every path this\npane sees arrives forward-slashed off the wire, and `join()` would not match on Windows", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/skills.test.ts", "before": "`.aiagent/skillset/...` starts with the letters but is a different directory.", "after": "`.aiagent/skillsets/…` starts with the same letters but is a different directory", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/skills.test.ts", "before": "The form opens on a blank to fill: a prompt that read as a finished request would let an\nauthor send a turn asking for \"a skill\" and nothing else.", "after": "The prompt ends on a blank to fill, because one that read as a finished request would let an\nauthor send a turn asking for \"a skill\" and nothing else", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/systemprompt.test.ts", "before": "The viewer's whole claim is that it shows what the agent is sent. The renderer cannot import\nthe node-side joiner, so the separator is written twice — and this is what keeps them equal.", "after": "The viewer claims to show what the agent is sent. The renderer cannot import the node-side\njoiner, so the separator is written twice and this test keeps the two spellings equal", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/taskGraph.test.ts", "before": "One picture, one box. A slot and the task filling it are the same thing seen from two sides,\nand drawing both would show the future twice — once as a promise and once as work.", "after": "A slot and the task filling it are the same thing seen from two sides, so the graph draws one\nbox for them rather than showing the same future twice, once as a promise and once as work", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/taskGraph.test.ts", "before": "The same reason `buildRefEdges` skips an author-supplied image: an edge to nowhere claims a\ncoupling the view cannot show either end of.", "after": "Skipped for the same reason `buildRefEdges` skips an author-supplied image: an edge to nowhere\nclaims a coupling the view cannot show either end of", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/taskGraph.test.ts", "before": "The gate stated as reachability rather than guessed at per kind: the sheet and the shot land\nbelow because aiko's portrait is upstream of both, and neither is named here.", "after": "The gate is stated as reachability rather than guessed at per kind: the sheet and the shot\nland below because aiko's portrait is upstream of both, and neither is named here", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/taskGraph.test.ts", "before": "The one case the walk cannot cover: the task was planned while the character was approved,\nand the approval was withdrawn afterwards, so its inputs no longer agree with the model.", "after": "The walk cannot cover this case: the task was planned while the character was approved, and\nthe approval was withdrawn afterwards, so the task's inputs no longer agree with the model", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/taskGraph.test.ts", "before": "The sheet hangs off the portrait's *task* node — the slot collapsed into it, so the edge\nhas to follow, or the promise would point at a box that is not on screen.", "after": "The sheet hangs off the portrait's task node, since the slot collapsed into it. The edge\nhas to follow, or the promise would point at a box that is not on screen", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/taskGraph.test.ts", "before": "The stated ceiling from the plan. This is a smoke bound, not a benchmark: it exists to\ncatch a derivation that goes quadratic, which is the way this gets slow.", "after": "The plan states this ceiling as a smoke bound rather than a benchmark: it catches a derivation\nthat has gone quadratic, which is the way this code becomes slow", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/tasklist.test.ts", "before": "The one tick an author reaches for after a run that did not produce what they expected — and\na shot that exhausted its refinements is the likeliest answer, so it is kept by a tick named\nfor failure rather than hidden by it.", "after": "This is the tick an author reaches for after a run that did not produce what they expected,\nand a shot that exhausted its refinements is the likeliest answer, so a tick named for failure\nkeeps it rather than hiding it", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/tasklist.test.ts", "before": "overlap — so any two on is a request for nothing, and answering nothing is correct.", "after": "overlap, so two ticks on is a request for nothing and answering nothing is correct", "labels": ["P9"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/tests/tasklist.test.ts", "before": "both on the filter used to be blamed — and told the author nothing had finished when the\nthree tasks it had just cleared all had.", "after": "both on, the filter used to be blamed, telling the author nothing had finished when what\nClear had just removed included the tasks that had", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/busy.ts", "before": "is about to replace — the surface locks instead, and {@link WRITE_PENDING} is both the refusal a", "after": "is about to replace. The surface locks instead, and {@link WRITE_PENDING} is both the refusal a", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/busy.ts", "before": "The bar itself waits {@link BUSY_DELAY_MS} before appearing: most commands land faster than a\nprogress bar can be read, and a flash on every retyped line would make the strip feel slower\nthan it is. The lock is immediate; only the *telling* is delayed.", "after": "The bar itself waits {@link BUSY_DELAY_MS} before appearing, because most commands land faster\nthan a progress bar can be read and a flash on every retyped line would make the strip feel\nslower than it is. The lock is immediate; only the notice is delayed.", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/busy.ts", "before": "The one sentence a locked surface has to say, wherever the author tries it. */", "after": "The sentence a locked surface says, shown at every control the lock covers. */", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/busy.ts", "before": "What the strip knows about the write in flight. `title` is the command's own name — the bar\nreads \"Moving shot…\", not a generic spinner — and `shown` is whether the delay has elapsed.", "after": "What the strip knows about the write in flight. `title` is the command's own name, so the bar\nreads \"Moving shot…\" rather than a generic spinner. `shown` is whether the delay has elapsed.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/busy.ts", "before": "The delay elapsed. Only a still-pending write becomes visible — a settled one stays settled. */", "after": "The delay elapsed. Only a still-pending write becomes visible; a settled one stays settled. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/busy.ts", "before": "What the progress bar says, or `null` while there is nothing to show — pending but not yet due. */", "after": "What the progress bar says. `null` when no write is pending, and until {@link BUSY_DELAY_MS} has\nelapsed.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/coverage.ts", "before": "The timeline's ghost geometry: how a proposed drag is drawn *over* the committed strip.", "after": "The timeline's ghost geometry, which is how a proposed drag is drawn over the committed strip.", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/coverage.ts", "before": "Everything else this module used to hold — `spansFor`, `resolveDrag`, `runsOf` — moved to\n`@vn/scriptedit` (`coverage.ts`) beside the rule they feed, because both hosts — this app and\nthe authoring agent — enumerate targets and settle drags with the same geometry. What stays\nhere is drawing: main has no use for a ghost.", "after": "`spansFor`, `resolveDrag` and `runsOf` moved out of this module to `@vn/scriptedit`\n(`coverage.ts`) beside the rule they feed, because both hosts (this app and the authoring agent)\nenumerate targets and settle drags with the same geometry. Only the drawing stays here, since\nmain has no use for a ghost.", "labels": ["P4", "P13"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/coverage.ts", "before": "targets: {@link TOP} above the first shot's midpoint, otherwise the last shot whose own midpoint\nis at or above the row.", "after": "targets. Answers {@link TOP} above the first shot's midpoint. Otherwise answers the last shot\nwhose own midpoint is at or above the row.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/coverage.ts", "before": "Midpoints rather than the boundaries between brackets, for the reason `dropTarget` gives in the\nscript column: the gap between two brackets is a hairline, and an insertion point the author can\nonly hit by accident is not one they can aim at. `spans` is in first-line order, so a single pass\nsettles it.", "after": "The test is on midpoints rather than on the boundaries between brackets, for the reason\n`dropTarget` gives in the script column: the gap between two brackets is a hairline, and an\ninsertion point the author can only hit by accident is not one they can aim at. `spans` is in\nfirst-line order, so a single pass settles it.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/coverage.ts", "before": "The row a drop after `target` would insert at — the row the marker is drawn above, and\n`rows.length` for a drop past the last line. {@link TOP} is row 0, since that is what an empty\n`after` means.", "after": "The row a drop after `target` would insert at, which is the row the marker is drawn above. A drop\npast the last line gives `rows.length`. {@link TOP} is row 0, since that is what an empty `after`\nmeans.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/coverage.ts", "before": "The ghost geometry for an in-flight drag, resolved against **committed** coverage.", "after": "The ghost geometry for an in-flight drag, resolved against committed coverage.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/coverage.ts", "before": "Deliberately not `spansFor` over mutated shots: lanes are greedy first-fit over *extents*, so", "after": "Deliberately not `spansFor` over mutated shots: lanes are greedy first-fit over extents, so", "labels": ["P10", "P2", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/coverage.ts", "before": "bracket keeps its geometry; only this overlay moves. It also keeps the grabbed handle under", "after": "bracket keeps its geometry; only this overlay moves. That also keeps the grabbed handle under", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/coverage.ts", "before": "Returns `null` for a shot that draws no bracket — it has no lane to draw a ghost in.", "after": "Returns `null` for a shot that draws no bracket, because it has no lane to draw a ghost in.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/drift.ts", "before": "How the strip says a frame no longer illustrates the words beside it. The derivation is main's\n(`@vn/pipeline`'s `driftOf`, surfaced on `CoverageShot.drift`); this is only the wording, kept\npure so it can be tested and so the task list can reuse the same sentences later.", "after": "How the strip says a frame no longer illustrates the words beside it. Main derives the drift\nitself (`@vn/pipeline`'s `driftOf`, surfaced on `CoverageShot.drift`); this module holds only the\nwording, kept pure so it can be tested and so the task list can reuse the same sentences later.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/drift.ts", "before": "Class suffix on the bracket, so how loud it is stays in the stylesheet. */", "after": "Class suffix on the bracket, so the prominence of the mark stays in the stylesheet. */", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/drift.ts", "before": "The whole of it, on hover — including what will and will not happen next. */", "after": "The full sentence shown on hover, including what the next run will and will not do. */", "labels": ["P4", "P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/drift.ts", "before": "Every shot rendered before the hash existed lands here, so it has to be quiet: the author\ncannot act on it, and it clears itself the next time the frame is made.", "after": "Every shot rendered before the hash existed lands here, so the mark stays quiet: the\nauthor cannot act on it, and the next render clears it", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/drift.ts", "before": "Drifted shots, for the bar. Only the actionable half is counted — `unknown` is a fact about the\nrecord rather than about the art, and a count of it would read as a count of faults.", "after": "Count of drifted shots, for the bar. `unknown` is deliberately left out: it is a fact about the\nrecord rather than about the art, and counting it would read as a count of faults.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/editing.ts", "before": "The pure half of the *strip's* two gestures: which one may start, and the one sentence a\nrefused grab has to say. Retyping itself — what a draft is as a line, what it commits, how a\nprecondition reads — is `src/shared/lineedit.ts`, shared with STUDIO's script column.", "after": "The pure half of the strip's two gestures: which one may start, and the sentence a refused grab\nis given. Retyping itself lives in `src/shared/lineedit.ts` (what a draft is as a line, what it\ncommits, how a precondition reads), shared with STUDIO's script column.", "labels": ["P13", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/editing.ts", "before": "The strip carries two gestures over one grid — dragging a bracket edge and retyping a line —\nand they cannot be live together: mid-drag the row under the pointer is read from the DOM and\nthe script column gives up its pointer events, which is exactly what an open editor needs.\nSo each one makes the other inert while it lasts (see {@link canEdit} / {@link canGrab}).", "after": "The strip carries two gestures over one grid (dragging a bracket edge and retyping a line) and\nthey cannot be live together. Mid-drag the row under the pointer is read from the DOM and the\nscript column gives up its pointer events, which is exactly what an open editor needs. Each\ngesture therefore makes the other inert while it lasts (see {@link canEdit} and {@link canGrab}).", "labels": ["P4", "P13"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/editing.ts", "before": "Every row's *text* is editable, whatever its kind. The speaker is not: changing who says a\nline changes its `kind`, hence the row's shape and the exporter's beat type. `story.setSpeaker`\nexists and belongs in STUDIO, where the script's structure is what you are editing.", "after": "Every row's text is editable, whatever its kind. The speaker is not: changing who says a line\nchanges its `kind`, and with it the row's shape and the exporter's beat type. `story.setSpeaker`\nexists and belongs in STUDIO, where the script's structure is what is being edited.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/editing.ts", "before": "What the strip is doing right now. All idle is the resting state. */", "after": "What the strip is doing right now. {@link IDLE} is the resting state. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/editing.ts", "before": "May a click open an editor? Not mid-drag: `.tl-grid.dragging` takes the script column's\npointer events away so the hit bands can be reached, and a click that lands nowhere is a\nbetter outcome than an editor opening under a held handle. And not mid-write: the row the\neditor would open over is about to be replaced by the re-read.", "after": "Whether a click may open an editor. Refuses mid-drag, because `.tl-grid.dragging` takes the\nscript column's pointer events away so the hit bands can be reached, and a click that lands\nnowhere is a better outcome than an editor opening under a held handle. Refuses mid-write,\nbecause the row the editor would open over is about to be replaced by the re-read.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/editing.ts", "before": "May a bracket handle be grabbed? Not while an editor is open, and not while a write is landing.", "after": "Whether a bracket handle may be grabbed. Refuses while an editor is open, and while a write is\nlanding.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/editing.ts", "before": "The plan's first answer was \"entering one closes the other\", which is worse: closing an editor", "after": "The rejected alternative was to have entering one gesture close the other: closing an editor", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/editing.ts", "before": "The sentence a refused grab is told. Two locks, two reasons: a write in flight outranks an open\neditor, because \"finish the line\" is bad advice while the strip is about to be rebuilt anyway.", "after": "The sentence a refused grab is given. A write in flight outranks an open editor, because \"finish\nthe line\" is bad advice while the strip is about to be rebuilt anyway.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/editing.ts", "before": "Why a handle does not move while an editor is open — and the one sentence in this strip that no\ncommand said, because no command was asked. A refused grab has to say something: the handle\ncannot take focus away from the editor (its `pointerdown` is prevented, so no blur, so no\ncommit), which means without this the click reads as the drag being broken.", "after": "Why a handle does not move while an editor is open. No command was invoked here, so no command\nsupplied this sentence. The handle cannot take focus away from the editor (its `pointerdown` is\nprevented, so there is no blur and no commit), so without this notice the click reads as a\nbroken drag.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/tests/busy.test.ts", "before": "The timer fires after the write already landed: the reveal must not resurrect the bar.", "after": "The timer fires after the write already landed, so the reveal must not bring the bar back", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/tests/coverage.test.ts", "before": "Above the first shot's midpoint is the one position no shot names: `top`, the empty `after`.", "after": "Above the first shot's midpoint no shot names the position, so the answer is the empty\n`after`, spelled `top`", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/tests/coverage.test.ts", "before": "The regression this function exists for. Deriving the preview with `spansFor` over mutated\nshots re-runs the greedy lane fit: growing `s__aiko` to the end of the scene gives it the\nwidest extent, which moves `s__establishing` — a shot the author never touched — into\nanother column and changes the grid's column count mid-gesture.", "after": "The regression this function exists for: deriving the preview with `spansFor` over mutated\nshots re-runs the greedy lane fit, so growing `s__aiko` to the end of the scene gives it the\nwidest extent, moving the untouched `s__establishing` into another column mid-gesture", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/tests/coverage.test.ts", "before": "The establishing shot covers L1 and L4; extending its start is a no-op, so extend the end.", "after": "Dragging aiko's start up to row 0 takes L1 from the establishing shot, so the proposal is\none unbroken run; the second case leaves a hole where L2 stays with aiko", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/tests/drift.test.ts", "before": "The whole point of the mark: no run is going to fix this on the author's behalf.", "after": "The mark exists to say that no run will fix this on the author's behalf", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/tests/editing.test.ts", "before": "commits it, and the second opens. Only the *other* gesture is locked out.", "after": "commits it, and the second opens. Only the grab gesture is locked out", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/tests/wardrobe.test.ts", "before": "Clearing the marker goes to the sheet, never to a shot: the shot level is below it.", "after": "Clearing the marker falls back to the character sheet, never to a shot; the shot level\nsits below the scene level", "labels": ["P4", "P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/tests/wardrobe.test.ts", "before": "The chain the whole feature is: the subject's own outfit, then the marker, then the sheet.", "after": "Resolution order is the subject's own outfit, then the scene marker, then the character sheet", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/tests/wardrobe.test.ts", "before": "A scene row is never shadowing, and neither is a shot row that says nothing.", "after": "A scene row never shadows, and a shot row left at INHERIT does not either", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/wardrobe.ts", "before": "The outfit strip's rows: who can be dressed here, what each control currently holds, and what\nclearing it would fall back to.", "after": "Rows of the outfit strip. Each row names a character that can be dressed, the value its control\ncurrently holds, and the value a clear would fall back to.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/wardrobe.ts", "before": "Two levels sit in one list because they answer one question — a scene row per cast member, a\nsubject row per subject of the selected shot — and the shot rows are the ones that shadow. The\ninheritance chain is not re-decided here: `outfitFor` is called for both the value in force and\nthe value a clear would reveal, so the strip and the prompt cannot disagree about either.", "after": "Both levels sit in one list: a scene row per cast member, and a subject row per subject of the\nselected shot. Only the shot rows shadow a scene marker. The inheritance chain is not re-decided\nhere. `outfitFor` supplies both the value in force and the value a clear would reveal, so the\nstrip and the prompt cannot disagree.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/wardrobe.ts", "before": "The select's value for \"say nothing here\"; an empty outfit is what both commands clear with. */", "after": "A select's value for setting nothing at this level. Both commands clear with an empty outfit. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/wardrobe.ts", "before": "Outfit ids the sheet authors, in order — what the select offers beside {@link INHERIT}. */", "after": "Outfit ids from the character sheet, in order; offered by the select beside {@link INHERIT}. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/wardrobe.ts", "before": "What this row itself says: an outfit id, or {@link INHERIT} when it says nothing. */", "after": "This row's own value: an outfit id, or {@link INHERIT} when the row sets nothing. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/wardrobe.ts", "before": "A scene row's own answer is the marker, and what it falls back to is the sheet — the shot\nlevel is below it and cannot be what a *scene* marker inherits from.", "after": "A scene row's value is the marker and its fallback is the character sheet; the shot level\nsits below the scene level, so a scene marker never inherits from it", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/wardrobe.ts", "before": "The scene marker a shot row is hiding, if it is hiding one. This is the only thing the strip\nsays about a shot decomposed before outfits were authorable: those carry an explicit outfit, so\na marker cannot reach them, and clearing the row is the fix. It deliberately does not try to\ntell a baked outfit from a deliberate one — the file asserts an override either way.", "after": "The scene marker a shot row hides, or null when it hides none. A shot decomposed before outfits\nwere authorable carries an explicit outfit that a marker cannot reach, and clearing the row is\nthe fix; this is all the strip says about such a shot. A baked outfit is not distinguished from\na deliberate one, because the file asserts an override either way.", "labels": ["P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/renderer/rules/timeline/wardrobe.ts", "before": "The invocation a row runs when its select changes — the command, not a description of it. */", "after": "The command invocation a row runs when its select changes. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "Assembling one difficult-agent analysis: the evidence, the redactor, the read tools, and the", "after": "Assembles one difficult-agent analysis from the evidence, the redactor, the read tools, and the", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "It sits beside `commandlog.ts` rather than inside `@vn/agentreport` because every line of it is\nabout *this host* — where a packaged build put its source, which account the app is running as,\nwhere fetched documentation may be cached. The package holds what is true of any host.", "after": "This lives beside `commandlog.ts` rather than inside `@vn/agentreport` because it is all about\nthe desktop host: where a packaged build put its source, which account the app runs as, and\nwhere fetched documentation is cached. The package holds what is true of any host.", "labels": ["P10", "C8"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "The model doing the reading, already resolved from the dialog or the bound one. */", "after": "The model doing the reading, already resolved from the dialog or from the bound model. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "Whether the author let the analyst read the requests the app sent — the bodies a positional\nAPI error indexes into. Independent of {@link source}: either alone is a valid analysis.", "after": "Whether the author let the analyst read the requests the app sent, which are the bodies a\npositional API error indexes into. Independent of {@link source}, and either alone is a valid\nanalysis.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "**Nothing read this way reaches the report.** It is the author's own conversation, read on\nthe author's own key, which is what keeps the privacy area to their model provider.", "after": "Nothing read this way reaches the report. The requests are the author's own conversation, read\non the author's own key, which keeps the privacy area to their model provider.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "documentation is cached. Absent in tests: nothing is written and every fetch goes out fresh.", "after": "documentation is cached. Absent in tests, where nothing is written and every fetch goes out\nfresh.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "`undefined` from `sourceRoot()` is a broken install, and the caller is told so by name. */", "after": "Reported when `sourceRoot()` returns `undefined`, which means a broken install. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "Keep a copy of the report beside the app's own files, never inside the project.", "after": "Keeps a copy of the report beside the app's own files, never inside the project.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "agent is not something to commit on their behalf — a bug report is about the *app*, not about\nthe story. Writing here is also why `report.agent` can stay `mutating: false`: nothing under the\nworktree moves, so commit-on-save is not involved at all.", "after": "agent should not be committed on their behalf. Writing outside the worktree is also why\n`report.agent` can stay `mutating: false`, since commit-on-save is never involved.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "A failed write is swallowed by the caller: the report is in hand either way, and losing the\narchive copy is not a reason to lose the analysis that was just paid for.", "after": "The caller swallows a failed write, because the report is in hand either way and losing the\narchive copy should not lose the analysis that was just paid for.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "Colons are not filenames on Windows, and the dots read as extensions everywhere.", "after": "Colons are illegal in Windows filenames, and the fractional-second dots read as extensions", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "The redactor for one loaded project: every name the fiction uses, plus the three things this\ncomputer gives away. `machine` is filled in here because the package is pure — the account name\nand the home directory are facts about the host, not about the project.", "after": "Builds the redactor for one loaded project, covering every name the fiction uses plus the\nidentifying details of this computer. `machine` is filled in here because the package is pure:\nthe account name and the home directory are facts about the host, not about the project.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "One redactor is one pseudonym table, so the caller keeps the one an analysis was written with:\nscanning a finished report with a *different* redactor would be scanning for different names.", "after": "Each redactor carries its own pseudonym table, so the caller keeps the one an analysis was\nwritten with. Scanning a finished report with another redactor would scan for different names.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "The read tools, in a context holding the two things `@vn/authoring`'s loop insists on. Nothing\nin the registry touches either — the tools resolve their own roots — but the loop's type says\na context has a workspace and a git, and satisfying it honestly costs nothing.", "after": "The read tools, in a context holding the workspace and git that `@vn/authoring`'s loop type\nrequires. Nothing in the registry uses either, because the tools resolve their own roots.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "The redactor is built before the evidence is read, and **the evidence is put through it here** —\nso what comes back is already clean, and the model, the rendered issue and the copy saved to\ndisk are all reading the same redacted value. That is what makes the privacy claim in the dialog\ntrue rather than aspirational: nothing downstream has to remember to redact, because there is\nnothing left to redact. It is handed back so the leak scan over the finished report runs against\nthe same table this was written with.", "after": "The redactor is built before the evidence is read, and the evidence is redacted here, so the\nmodel, the rendered issue and the copy saved to disk all read the same redacted value. Nothing\ndownstream has to remember to redact. The redactor is returned so the leak scan over the\nfinished report runs against the same pseudonym table the report was written with.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "Frozen before anything is read, and before the analyst's own turns could move it — evidence\nthat shifts under its reader is not evidence.", "after": "Frozen before anything is read, and before the analyst's own turns could add to the ring", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "One analysis, one budget, however many kinds of reading it does.", "after": "One budget covers the whole analysis, across every kind of reading it does", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/agentreport.ts", "before": "A detail-only run has no source root to take a context from, so it brings its own. The\nrequest tools never touch either half of it; the loop's type is what asks for one.", "after": "A detail-only run has no source root to take a context from, so it supplies its own. The\nrequest tools use neither the workspace nor the git; only the loop's type asks for them.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetlabel.ts", "before": "sidebar and the backlink panel say `Aiko — uniform / front` and keep the hash for when two\nassets would otherwise answer to the same words. Pure: every fact comes from the caller.", "after": "sidebar and the backlink panel say `Aiko — uniform / front` and fall back to the hash when two\nassets would otherwise carry the same words. Pure: every fact comes from the caller.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetlabel.ts", "before": "The model-sheet angle a task was for. `Asset.satisfies` binds only `{characterId, outfit}`\n— four sheets share that binding and differ only here.", "after": "The model-sheet angle a task was for. `Asset.satisfies` binds only `{characterId, outfit}`,\nso four sheets share that binding and differ only in the angle.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetlabel.ts", "before": "The context a loaded project answers with — the model for names, the graph for angles. */", "after": "Builds the context from a loaded project: the model supplies names, the graph supplies angles. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetlabel.ts", "before": "The fallback name, and the suffix a collision gets: the first 8 of the hash. */", "after": "First 8 characters of the hash, used as the fallback name and as a collision suffix. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetlabel.ts", "before": "One asset's display name, or `undefined` when nothing in the model claims it — an asset whose\ncharacter has since been deleted has no name to give, and inventing one would be a lie about\nwhat the manifest says.", "after": "One asset's display name, or `undefined` when nothing in the model claims it. An asset whose\ncharacter has since been deleted has no name, and none is invented for it.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetlabel.ts", "before": "An upload's name is the one an author typed, or the file they picked. It is bound to nothing\nby construction — nothing generated it — so a binding is never what names it.", "after": "An upload's name is the one an author typed, or the file they picked. Nothing generated an\nupload, so it carries no binding and a binding never names it", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetlabel.ts", "before": "A concept is the other kind whose name was authored rather than derived, and the other that may\nlegitimately be bound to nothing — so it answers before the binding is required.", "after": "A concept's name is also authored rather than derived, and a concept may legitimately carry no\nbinding, so the concept case is handled before a binding is required", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetlabel.ts", "before": "Every asset's label, hash → label. Two assets that land on the same words both keep a\n`(hash8)` suffix, so a label is never quietly ambiguous; one with no label at all falls back\nto the `hash8.ext` the tree showed before names existed.", "after": "Every asset's label, keyed by hash. Two assets that produce the same words both get a `(hash8)`\nsuffix, so a label is always unambiguous. An asset with no label falls back to `hash8.ext`,\nwhich is what the tree showed before names existed.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetprompt.ts", "before": "The prompt an asset would be generated with *today*, re-derived from the current inputs.", "after": "The prompt an asset would be generated with now, re-derived from the current inputs.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetprompt.ts", "before": "The planner does this on every pass and folds the result into the task hash, so an asset whose\nrecorded prompt no longer matches this one was rendered from words the project has since\nchanged — which is exactly what an art-notes edit produces, and the one thing the asset editor\nhas to be able to say out loud. Deliberately a re-derivation rather than a stored flag: the\nbuilders are the authority on what a prompt is, and a flag would go stale the first time one\nof them improved.", "after": "The planner derives the same prompt on every pass and folds it into the task hash, so an asset\nwhose recorded prompt no longer matches was rendered from words the project has since changed.\nAn art-notes edit produces exactly that, and the asset editor reports it. This is a\nre-derivation rather than a stored flag because the builders are the authority on what a prompt\nis, and a flag would go stale as soon as a builder changed.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetprompt.ts", "before": "The clauses `asset`'s prompt is built from today, or `undefined` on the same terms as\n{@link derivePrompt}. The builders' own order, with no override applied — `composePrompt` is\nwhat turns these into what would be sent.", "after": "The clauses `asset`'s prompt is built from now, or `undefined` on the same terms as\n{@link derivePrompt}. The chunks come back in the builders' own order with no override applied;\n`composePrompt` turns them into the text that would be sent.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetprompt.ts", "before": "A concept has no builder and no task: its prompt was a sentence somebody typed once, so there\nis nothing to re-derive and it can never be stale.", "after": "A concept has no builder and no task. Its prompt was authored rather than derived, so there is\nnothing to re-derive and it never goes stale", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetprompt.ts", "before": "longer describes this asset at all (its character was deleted, its storyboard is gone). Absent\nis not \"unchanged\" — a caller comparing against the recorded prompt must treat it as unknown.", "after": "longer describes this asset at all (its character was deleted, its storyboard is gone). A caller\ncomparing against the recorded prompt must read `undefined` as unknown rather than as unchanged.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/assetprompt.ts", "before": "The author's override is part of \"as the builders would write it now\": it is what the planner\nfolds into the task hash, so a derivation that ignored it would report every overridden asset\nas drifting from itself.", "after": "The author's override is included, because the planner folds it into the task hash. A derivation\nthat ignored the override would report every overridden asset as drifting from itself.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commandlog.ts", "before": "Nothing else in the app has needed this: provenance was written to be read by a person with a\ntext editor. A difficult-agent report is the first reader in code, and it wants the acting\nrecord a transcript does not have.", "after": "Nothing else in the app has needed this, because provenance was written to be read by a person\nwith a text editor. The difficult-agent report is the first reader in code, and it needs the\nrecord of what was done, which a transcript does not carry.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commandlog.ts", "before": "A half-written last line is what a crash mid-append leaves behind, and a line missing `seq` is\nnot a record at all — both are skipped rather than thrown over, for the same reason a thread\nstill lists when its final line was cut short.", "after": "A crash mid-append leaves a half-written last line, and a line missing `seq` is not a record.\nBoth are skipped rather than thrown over, the same way a thread still lists when its final line\nwas cut short.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commandlog.ts", "before": "A line that will not parse is a line that was never finished being written.", "after": "A line that fails to parse was never finished being written", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commandlog.ts", "before": "One conversation and the acts around it, ready for the analyst. The join itself is pure and", "after": "One conversation and the commands around it, ready for the analyst. The join itself is pure and", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/agent.ts", "before": "The authoring agent as commands. `agent.run` is marked mutating because a turn in execute\nmode can edit files — but the agent keeps its own plan/execute gate, which this does not\nbypass: the command is just another way to hand it a turn.", "after": "The authoring agent as commands. `agent.run` is marked mutating because a turn in execute mode\ncan edit files. The agent keeps its own plan/execute gate, and these commands do not bypass it.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/agent.ts", "before": "selection to speak of, and a turn with no scene in view is a turn like any other.", "after": "selection, and a turn runs the same way with no scene in view", "labels": ["P9"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/agent.ts", "before": "The window whose composer sent this turn is where the turn's cards belong. Without this\nnote the ask went to whichever window held focus when it fired — usually right, until the\nauthor is reading in another window, at which point the card lands where they are not.", "after": "The turn's cards belong to the window whose composer sent it. Without this note an ask goes\nto whichever window holds focus, which lands the card away from the author once they are\nreading in another window", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/agent.ts", "before": "The turn it interrupts owns whatever was written, and is where the undo point already is.", "after": "The interrupted turn accounts for whatever it wrote, and already holds the undo point", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/agent.ts", "before": "Every choice is accepted, not just the ones the current model offers: the menu is what\nfilters, and a level the model will not take is stepped down at the wire, not refused.", "after": "Every choice is accepted, not just the ones the current model offers. The menu does the\nfiltering, and a level the model will not take is stepped down at the wire rather than refused", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/agent.ts", "before": "Kept in the install's session file rather than the project's: it is a spending decision\nabout this machine and this author, not something a collaborator inherits with the repo.", "after": "Kept in the install's session file rather than the project's, because the ceiling is a\nspending decision about this machine and author that a collaborator does not inherit", "labels": ["P4", "P2"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/agent.ts", "before": "The four thread commands. None is `undoable`: `vngen/state` is outside the undo snapshot by\ndesign, so a journal entry claiming to restore a transcript could not — and `agent.renameThread`\nis `mutating` only because it writes a file, which is also why it is the only one of the four\nthat is.", "after": "The four thread commands. None is `undoable`, because `vngen/state` sits outside the undo\nsnapshot, so a journal entry could not actually restore a transcript. `agent.renameThread` is\nthe only one marked `mutating`, and only because it writes a file.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/agent.ts", "before": "What renaming would hit — the open thread when no id is named, and nothing when none is. */", "after": "Decides what a rename would target: the named thread, or the open one when no id is given. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/app.ts", "before": "The app itself as commands — the few acts that are about VN Studio rather than about the\nproject it happens to have open.", "after": "The app itself as commands, covering the few acts that are about VN Studio rather than about the\nproject it has open.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/app.ts", "before": "the agent then reach one implementation, and a pane that fetched its own words over a bespoke\nchannel would be a second way to ask the same question.", "after": "the agent all reach one implementation, instead of a pane fetching its own words over a bespoke\nchannel.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/app.ts", "before": "A guide that is missing a section is still returned — the pane draws what there is. The\nmessage is what says so, because the alternative is a pane that is quietly short.", "after": "A guide missing a section is still returned and the pane draws what there is. The message\nreports the gap so the pane is not silently short", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/app.ts", "before": "check is deliberately not a thrown error — see `announcementFor`.", "after": "check is reported in the verdict rather than thrown; see `announcementFor`", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/art.ts", "before": "Art as commands: what it should look like, and drawing one on demand.", "after": "Commands for art direction and for drawing a concept image on demand.", "labels": ["P4", "P6", "P11"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/art.ts", "before": "Art notes are the only thing an author says about how generated art should *look*, and they are\nauthored input rather than a prompt override: they go into the prompt the builders derive, so\nsetting one re-keys exactly the tasks it reaches and the next run re-renders them. That is why\n`art.setNotes` is a `story.*`-shaped document mutator — undoable, committed — and not a pipeline\ncommand. `art.generate` is the other half: the door the planner does not have, one sentence in\nand one `concept` asset out, planned by nothing and consumed by nothing.", "after": "Art notes are the only thing an author says about the appearance of generated art, and they are\nauthored input rather than a prompt override. They go into the prompt the builders derive, so\nsetting a note re-keys exactly the tasks it reaches and the next run re-renders them. That\nmakes `art.setNotes` a `story.*`-shaped document mutator (undoable and committed) rather than a\npipeline command. `art.generate` takes one sentence and produces one `concept` asset, which the\nplanner never plans and nothing downstream consumes.", "labels": ["P13", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/art.ts", "before": "A session preview read as a precondition: the plan's own sentence either way. */", "after": "Turns a session preview into a check result, keeping its message as the note or the refusal. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/art.ts", "before": "-1 rather than a second prop: 0 is a real seed, so no in-range value is free to mean \"none\".", "after": "-1 rather than a second prop, because 0 is a real seed and no in-range value can mean none", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/art.ts", "before": "The prop's sentinel as the session states it: `null` is \"author no seed here\". */", "after": "Maps the prop's negative sentinel to the `null` the session uses for no seed at this rung. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/art.ts", "before": "It spends a real image call, which is the bar `asset.regenerate(run=true)` clears too. Not\nundoable and not journalled: it writes new content-addressed bytes, so there is no prior\nstate to restore to.", "after": "Spends a real image call, the same bar `asset.regenerate(run=true)` clears. It is neither\nundoable nor journalled, because it writes new content-addressed bytes and there is no prior\nstate to restore", "labels": ["C8"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/art.ts", "before": "The same route a click on an asset in the document tree takes, so the picture lands in a\npane that is not the one that asked for it.", "after": "Same route as a click on an asset in the document tree, so the picture opens in a pane other\nthan the one that asked for it", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/art.ts", "before": "One image call, like `art.generate`. Not undoable for the same reason: new bytes have no\nprior state to restore to.", "after": "One image call, like `art.generate`, and not undoable because new bytes have no prior state\nto restore", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/art.ts", "before": "It writes a sheet, a manifest row and a `done` task record across two trees, which no\ndocument snapshot covers — so it is committed like any act, but never undone.", "after": "Writes a sheet, a manifest row and a `done` task record across two trees, which no document\nsnapshot covers, so it is committed like any other act but never undone", "labels": ["C8"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/asset.ts", "before": "One generated asset as commands: what it is, accepting it, re-rendering it.", "after": "Commands over one generated asset: reading it, accepting it, re-rendering it.", "labels": ["P11"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/asset.ts", "before": "the manifest is the one place every kind of generated art meets. What an asset *should look\nlike* is not here: that is authored input, and it is `art.setNotes`.", "after": "the manifest is the one place every kind of generated art meets. What an asset should look like\nis authored input instead, and lives in `art.setNotes`.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/asset.ts", "before": "A session preview read as a precondition: the plan's own sentence either way. */", "after": "Turns a session preview into a check result, keeping the preview's message in both cases. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/asset.ts", "before": "It writes bytes into the repo from a path the author named, which is worth one confirmation.", "after": "Writes bytes into the repo from a path the author named, which is worth one confirmation", "labels": ["P6", "C8"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/asset.ts", "before": "It changes what the project's art *is*, and with `replace` it supersedes real work.", "after": "Makes existing bytes the project's art, and with `replace` it supersedes real work", "labels": ["P10", "C8"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/asset.ts", "before": "It supersedes real work with a file from outside, which is the bar `asset.upload` clears too.", "after": "Supersedes real work with a file from outside, which is the bar `asset.upload` clears too", "labels": ["P6", "C8"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/asset.ts", "before": "The chooser is not a permission: what the command would refuse is refused after it too.", "after": "Choosing a file grants no permission; the same refusals apply after the chooser closes", "labels": ["P4", "P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/asset.ts", "before": "Whether a requeue runs the pipeline on its own, and why — `''` for \"it does not\".", "after": "Why a requeue runs the pipeline on its own, or `''` when it does not.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/asset.ts", "before": "The author's own reading: one queued task is a re-roll, and sending them to the run dialog to\npress Run is ceremony. Anything wider stays a decision, a gate is a halt rather than a run, and\na run that cannot resolve its keys is not started — `runPreconditions` names the source that\nfailed, never the value.", "after": "A single queued task runs without asking, because it is a re-roll. More than one pending task\nstays the author's decision, a closed gate halts rather than runs, and a run whose keys do not\nresolve is not started. `runPreconditions` names the source that failed, never the value.", "labels": ["P4", "P5"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/bible.ts", "before": "The story bible as a command. Read-only and budgeted: there is no `bible.read`, because", "after": "The one command over the story bible, read-only and budgeted. There is no `bible.read` because", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/catalog-entry.ts", "before": "Verifying here is deliberate: it is the earliest place both registries exist, and it runs at", "after": "Verification runs here because this is the earliest place both registries exist, and it runs at", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/command.ts", "before": "The command surface asking about itself: would that invocation run?", "after": "A command that reports whether another invocation would run.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/command.ts", "before": "It takes a DSL line rather than an id and a prop bag because the answer is about a *specific*\nact — `story.setNext(scene='a' goto='b')` refuses where `goto='c'` accepts — and the DSL is\nalready how an invocation is written down everywhere else (history, the palette, CDP).", "after": "It takes a DSL line rather than an id and a prop bag because the answer depends on the exact\narguments — `story.setNext(scene='a' goto='b')` refuses where `goto='c'` accepts — and the DSL\nis already how an invocation is written down everywhere else (history, the palette, CDP).", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/doc.ts", "before": "Authored markdown documents as commands: the wiki, character sheets, location sheets. The", "after": "Commands over authored markdown documents: the wiki, character sheets, location sheets. The", "labels": ["P11"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/doc.ts", "before": "authored in either place — naming half of what these reach would be a lie about the other half.", "after": "authored in either place, so a name covering only one of the two would misdescribe the other.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/doc.ts", "before": "written whole is unvalidated, and the refusal is the same code the agent's `write_file` runs.", "after": "written whole is unvalidated, and the agent's `write_file` refuses it through this same code.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/doc.ts", "before": "A `DocResult` read as a precondition: the plan's own sentence either way. */", "after": "Turns a `DocResult` into a check result, keeping its message in both cases. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/doc.ts", "before": "A page scaffolded and then left unfindable is a page the author has to go looking for, so\nthe same route a click in the document tree takes is run for them. All three kinds land in\nthe Wiki editor: a sheet is markdown too, and `EDITORS` claims it there.", "after": "Shows the new page by the same route a click in the document tree takes, so the author does\nnot have to go looking for it. Every kind lands in the Wiki editor, because a sheet is\nmarkdown too and `EDITORS` claims it there", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/gate.ts", "before": "The character-approval gate (P3) as commands: inspect candidates, approve one. */", "after": "Commands over the character-approval gate (P3): inspect candidates, approve one. */", "labels": ["P11"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/gate.ts", "before": "An empty hash is the ordinary way in, not a typo: the tasks and graph editors open this\nform on a *character* and leave which portrait to the author. \"has no candidate ''\" reads\nlike a lookup that failed, so the unanswered field is named instead.", "after": "An empty hash is ordinary rather than a typo, because the tasks and graph editors open\nthis form on a character and leave the choice of portrait to the author. \"has no candidate\n''\" would read as a failed lookup, so the refusal names the unanswered field instead", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/gate.ts", "before": "Already approved is not a refusal: approving a second candidate is how an author\nchanges their mind, and the command supports it.", "after": "Already approved is not a refusal, because an author changes their mind by approving a\nsecond candidate and the command supports that", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/host.ts", "before": "How a file chooser is dressed. Everything is optional: the defaults are the document upload. */", "after": "Presentation of a file chooser. Everything is optional; the defaults are the document upload. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/host.ts", "before": "How a directory chooser is dressed. The defaults open a project; browsing for one does not. */", "after": "Presentation of a directory chooser. The defaults are worded for opening a project, so a\nchooser that only browses for a folder overrides them.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/host.ts", "before": "Persisted UI state. Named `state`, not `session`, to keep it distinct from the above. */", "after": "Persisted UI state. Named `state` to keep it distinct from the `session` field. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/host.ts", "before": "effect means *here*, in the window whose palette or menu ran the command. Omitted (or a\nwindow that has since closed) falls back to the focused window, which is what the agent,\nCDP and main itself get.", "after": "effect applies to the window whose palette or menu ran the command. An omitted target (or one\nnaming a window that has since closed) falls back to the focused window, which is what the\nagent, CDP and main itself get.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/host.ts", "before": "A report about this instant: `openWorkspace` re-acquires for real and refuses again.", "after": "The answer describes this instant only; `openWorkspace` re-acquires for real and refuses again.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/host.ts", "before": "no `ctx.origin`, and anything it remembers *about a window* has to be remembered about the\nsame one the effect reaches — otherwise `view.applyLayout` rearranges window 3 and writes\nwindow 0's template key.", "after": "no `ctx.origin`, and per-window state has to be recorded against the same window the effect\nreaches — otherwise `view.applyLayout` rearranges window 3 and writes window 0's template key.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/interaction.ts", "before": "The gesture surface, as commands: what interactions exist, and which targets would take one.", "after": "Commands over the gesture surface: which interactions exist, and which targets would take one.", "labels": ["P11"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/interaction.ts", "before": "`interaction.targets` is the point of the whole layer — it answers \"what would happen if I\ndropped this there, and why not\" *before* anything is attempted, using the same `targets`\nthe branch editor runs mid-drag. Both are non-mutating: an interaction never writes, it only\nnames the command that would.", "after": "`interaction.targets` answers what would happen if a carried thing were dropped on a target,\nand why a target refuses, before anything is attempted, using the same `targets` the branch\neditor runs mid-drag. Both commands are non-mutating, because an interaction never writes; it\nonly names the command that would.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/interaction.ts", "before": "`prompt.*` is judged against one asset's chunks **in effective order**, which is what", "after": "`prompt.*` is judged against one asset's chunks in effective order, which is what", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/notify.ts", "before": "The notification log as a vocabulary. Reading it is an IPC channel (`notify:list`), but every\n*change* to it is a command, because commands are the only write path — which also means the", "after": "Commands over the notification log. Reading it is an IPC channel (`notify:list`), but every\nchange to it is a command, because commands are the only write path — which also means the", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/notify.ts", "before": "outside `UNDO_PATHS`, so a journal entry claiming to restore a flag could not. They are\n`mutating: false` for the same reason — nothing under the undo snapshot changes — with the\nsingle exception of the one that destroys the file.", "after": "outside `UNDO_PATHS`, so a journal entry could not restore a flag. They are `mutating: false`\nfor the same reason — nothing under the undo snapshot changes — apart from the one that\ndestroys the file.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/notify.ts", "before": "\"Clear\" takes the ids the dialog drew rather than recomputing what is visible. The category\nfilter lives in the session store and only the dialog knows what it is showing, so asking main\nto re-derive it is how a Clear ends up hiding something that was filtered off screen.", "after": "Takes the ids the dialog drew rather than recomputing what is visible. The category filter lives\nin the session store and only the dialog knows what it is showing, so re-deriving the list in\nmain would let a Clear hide something that was filtered off screen.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/notify.ts", "before": "It truncates a file the repo tracks, so it is an act the committer should record — and, being\nthe only mutator here, the only one with a precondition worth asking about.", "after": "Truncates a file the repo tracks, so the committer should record it. This is the only mutator\nin this file, and the only one with a precondition worth asking about", "labels": ["C8"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/notify.ts", "before": "Clicking a notification: read, then go where it points. A notification with no link — or with\none naming an editor this build does not have, or a command it does not allow — is still marked\nread, and says it went nowhere rather than refusing: the click did do something.", "after": "Marks a notification read, then goes where it points. A notification with no link — or with one\nnaming an editor this build does not have, or a command it does not allow — is still marked read\nand reports that it went nowhere rather than refusing, because the click did do something.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/notify.ts", "before": "A link naming a command is followed *first*, through the same session method the command itself\nwraps — one implementation, two thin doors, exactly as `app.openKeyLink` sits over\n`session.openKeyLink`. That is what keeps a notification from ever being a URL: the log names\nan act and the app still derives the destination. `linkCommand` narrows against a short\nallow-list, because these lines arrive from other people's clones.", "after": "A link naming a command is followed before a link naming an editor, through the same session\nmethod the command itself wraps, the way `app.openKeyLink` sits over `session.openKeyLink`. A\nnotification therefore never carries a URL: the log names an act and the app derives the\ndestination. `linkCommand` narrows against a short allow-list, because these lines arrive from\nother people's clones.", "labels": ["P10", "P9"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "Deliberately not `confirm`. Every door to this — the header's button, the menu entry, the\nadvanced dialog's OK — is already a deliberate click on the words \"run pipeline\", and a\nsecond card saying them again was one the author dismissed without reading.", "after": "Deliberately not `confirm`. The header's button, the menu entry and the advanced dialog's OK\nare each already a deliberate click on the words \"run pipeline\", so a second card repeating\nthem gets dismissed without being read.", "labels": ["P2", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "The session runs one long thing at a time, and a second run would plan against a graph\nthe first is still writing. Named, because \"an agent turn\" is the case worth waiting out.", "after": "The session runs one long thing at a time, and a second run would plan against a graph the\nfirst is still writing. The refusal names what is busy, because \"an agent turn\" is the case\nworth waiting out.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "Pending work is reported, never refused: planning is incremental, so \"nothing pending\"\nmeans nothing is plannable *yet* — the run itself is what discovers the rest.", "after": "Pending work is reported, never refused. Planning is incremental, so \"nothing pending\" means\nnothing is plannable yet, and the run itself is what discovers the rest.", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "Planning is incremental, so the count above is what is plannable *now*.", "after": "Planning is incremental, so the planned count covers only what is plannable at this point", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "Before the await, not after: a run is the one command whose whole output is a list that\nfills in while it happens, and a task list that arrives once it is over is a receipt. The\nrenderer focuses the pane already showing the list rather than making a second one, so an\nauthor who keeps the Tasks editor docked never sees a popup at all.", "after": "Opened before the await: the task list fills in while the run happens, so opening it\nafterwards would show only the finished result. The renderer focuses a pane already showing\nthe list rather than making a second one, so a docked Tasks editor never becomes a popup.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "Counted from the plan, so a failure inherited from an earlier run still shows: without\nthis the palette reported a clean run over art that does not exist.", "after": "Counted from the plan, so a failure inherited from an earlier run still shows. Counting only\nthis run's own failures reported a clean run over art that does not exist.", "labels": ["P2"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "back. A ceiling rather than a target: each round unlocks the next rung of the slot graph —\nportrait, then sheet, then plate, then shot — so a project still going after this many is a\nproject with something wrong in it, and an unbounded loop would spend real image calls\ndiscovering that.", "after": "back. A ceiling rather than a target: each round unlocks the next rung of the slot graph\n(portrait, then sheet, then plate, then shot), so a project still going after this many rounds\nhas something wrong in it, and an unbounded loop would spend real image calls discovering that.", "labels": ["P13"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "Split out and pinned by tests because it is the whole feature: every branch is one that either\nspends more real image calls or stops short of finishing the art, and an author only finds out\nwhich afterwards.", "after": "Split out and pinned by tests because every branch either spends more real image calls or stops\nshort of finishing the art, and an author only finds out which afterwards.", "labels": ["P4", "P3", "P2"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "The convergence test: nothing was waiting to be approved and the planner had nothing left to\ndo. Said two ways, because a project whose remaining tasks are failed or flagged `needs_human`\nhas also stopped moving, and telling an author that everything is generated would be a lie\nthey only find out about by looking at the art.", "after": "Convergence: nothing was waiting to be approved and the planner had nothing left to do. A\nproject whose remaining tasks are failed or flagged `needs_human` has also stopped moving, so\nthat case gets its own sentence instead of being reported as everything generated.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "The rows to approve this round: every unblocked candidate for a slot **nothing has settled yet**,\nand at most one per slot.", "after": "The rows to approve this round: every unblocked candidate for a slot nothing has settled yet, and\nat most one per slot.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "Both halves are the same rule, and both are about a slot's candidates being *alternatives* rather", "after": "Both halves are the same rule, and both are about a slot's candidates being alternatives rather", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "A portrait of nobody clears nobody from the gate — `approveOne` says exactly that, and\nskipping it here keeps the loop from asking once a round for as long as it runs.", "after": "A portrait with no character clears nobody from the gate, which is the sentence `approveOne`\nrefuses with. Skipping it here keeps the loop from asking about it again every round.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "Every step is undoable on its own and the pass is not undoable as a whole, which is exactly\nwhen a confirmation earns its place.", "after": "Every step is undoable on its own; the pass as a whole is not.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "Said before the pass rather than after it: a project with nothing waiting and nothing\nplannable ends on its first round, and an author who was not told that reads the round it\ntakes as a pass that ran and did nothing.", "after": "A project with nothing waiting and nothing plannable ends after one round. The check says so\nbeforehand, because otherwise that round reads as a pass that ran and did nothing.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/pipeline.ts", "before": "happens for a while. Opened before the first await, so it is company rather than a receipt.", "after": "happens for a while. Opened before the first await, so it is visible throughout the pass.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/project.ts", "before": "looks like, and the bill arrives at the next run.", "after": "looks like, and the next run redraws the whole library for it.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/project.ts", "before": "It writes nothing. It does spend, which the description says, because a command that quietly\ncosts money is one an author should have been told about rather than have to infer.", "after": "Writes nothing of its own. It does spend money, which the description states so that an author\nis not left to infer the cost.", "labels": ["C8"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/prompt.ts", "before": "A separate namespace from `art.*` on purpose: `art.setNotes` writes **authored input that feeds\nthe derivation** — it lands in the sheet and the builders read it back — while everything here\nwrites an **override of the derivation**, a block that says what to do to the clauses the\nbuilders produced. Different act, different place on disk, different thing to undo.", "after": "A separate namespace from `art.*` on purpose: `art.setNotes` writes authored input that feeds the\nderivation (it lands in the sheet and the builders read it back) while everything here writes an\noverride of the derivation, a block that says what to do to the clauses the builders produced.\nDifferent act, different place on disk, different thing to undo.", "labels": ["P13", "P10", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/prompt.ts", "before": "Not digested: a clause is a sentence, and the history line reading back what was written is\nthe record. Digesting is for a whole document (`prompt.setCustom`).", "after": "A clause is a sentence, so it is not digested: the history line reading back what was\nwritten is the record. Digesting is for a whole document (`prompt.setCustom`).", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/report.ts", "before": "`digest: true` is about the *record*, not the form: a report is thousands of words of\nsomeone's conversation, and `commands.jsonl` is committed. The preview edits it elsewhere.", "after": "`digest: true` is about the record, not the form: a report is thousands of words of\nsomeone's conversation, and `commands.jsonl` is committed. The report preview dialog is\nwhere the body is edited.", "labels": ["P10", "P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/story.ts", "before": "The same decision `apply` would make, against a freshly read graph, and then discarded. It is\nthe rule itself rather than a description of it — the price is that the check reads the story\nonce more, which is what makes it a report about *now* rather than about the last load.", "after": "The same decision `apply` would make, against a freshly read graph, and then discarded. The check\nruns the rule rather than describing it; the price is that it reads the story once more, which is\nwhat makes it a report about the story as it stands rather than about the last load.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/story.ts", "before": "The preview runs the *whole* plan rather than just the decision, because the sentence worth", "after": "The preview runs the whole plan rather than just the decision, because the sentence worth", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/story.ts", "before": "The two outfit commands. Neither goes through `preview`: that decides against the story graph,\nwhich carries edges and reachability and not a word about clothes.", "after": "The two outfit commands. Neither goes through `preview`, which decides against the story graph,\nand the graph carries edges and reachability but nothing about clothes.", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/story.ts", "before": "A model call per scene, and the result is a file that wins forever. Both halves of that\ndeserve the dialog.", "after": "Confirmed because it costs a model call per scene, and because the storyboard it writes is\nnever regenerated afterwards.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/story.ts", "before": "Named, not counted: an unwritten scene is the one thing here an author has to act on.", "after": "An unwritten scene is the one thing here an author has to act on, so the scenes are listed\nby name rather than counted", "labels": ["P4", "P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/story.ts", "before": "Building the playable *is* the question — it is pure and writes nothing, so the check\ncan answer with the real projection rather than a guess about whether one would work.", "after": "Building the playable is the question, and it is pure and writes nothing, so the check\nanswers with the real projection rather than a guess about whether one would work.", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/approveandrun.test.ts", "before": "The same standstill, told honestly: a project whose last tasks are failed or `needs_human`\nhas stopped moving too, and \"everything is generated\" is a claim the author disproves by\nopening the art.", "after": "A project whose last tasks are failed or flagged `needs_human` has stopped moving too, so it\nmust not be reported as everything generated — the author would disprove that by opening the\nart.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/approveandrun.test.ts", "before": "The bug this whole rule exists for: a finished project still lists the takes that lost, and a\npass that approved them would change every settled answer and be offered the old one next\nround, forever, while the pipeline it ran between rounds had nothing to do.", "after": "A finished project still lists the takes that lost. A pass that approved them would change\nevery settled answer, be offered the old one again next round forever, and leave the pipeline\nit ran between rounds with nothing to do.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/autorun.test.ts", "before": "When a requeue runs the pipeline by itself. The decision is the whole feature, and the act it\nguards spends a real image call, so it is pinned here rather than left to a live run.", "after": "When a requeue runs the pipeline by itself. The act this decision guards spends a real image\ncall, so the decision is pinned here rather than left to a live run.", "labels": ["P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/autorun.test.ts", "before": "Nothing pending is not \"one task\": the requeue is what should have put it there.", "after": "Zero pending does not count as the one task, because the requeue is what should have\nplanned that task", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/commands.test.ts", "before": "Undo restores a snapshot of the *document* tree, so only commands whose writes are\ndocuments may opt in. The rest write generated output (`story.export`, `story.screenplay`,", "after": "Undo restores a snapshot of the document tree, so only commands whose writes are documents may\nopt in. The rest write generated output (`story.export`, `story.screenplay`,", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/commands.test.ts", "before": "`asset.regenerate`), straddle a sheet, the manifest and the task log at once\n(`art.promote`, and `asset.adopt`/`asset.replace`, which append a `done` record the log has\nno un-appending for), restructure the whole worktree\n(`workspace.import`, whose own `.imported` rename is the reversal), write into a *different*\ntree than the one a snapshot covers (`workspace.open`/`pick`/`create`), copy bytes in from\noutside the tree *and* close the conversation a snapshot cannot restore (`upload.*`), write under\n`vngen/state`, which the snapshot deliberately excludes (`agent.renameThread` — a transcript\nmust survive undoing the edits it produced), write a credential to a *gitignored* file\n(`project.setKey`: an undo point is a git snapshot, and snapshotting a key is the one thing\nthat command exists to avoid), or straddle both\nclasses (`gate.approve` flips\n`character.md` **and** marks the asset accepted in `manifest.json`) — see\n`docs/plans/archive/command-undo-redo.md`.", "after": "`asset.regenerate`), straddle a sheet, the manifest and the task log at once (`art.promote`,\nand `asset.adopt`/`asset.replace`, which append a `done` record the log has no un-appending\nfor), restructure the whole worktree (`workspace.import`, whose own `.imported` rename is the\nreversal), write into a different tree than the one a snapshot covers\n(`workspace.open`/`pick`/`create`), copy bytes in from outside the tree and close the\nconversation a snapshot cannot restore (`upload.*`), write under `vngen/state`, which the\nsnapshot deliberately excludes (`agent.renameThread` — a transcript must survive undoing the\nedits it produced), write a credential to a gitignored file (`project.setKey`: an undo point is\na git snapshot, and snapshotting a key is the one thing that command exists to avoid), or\nstraddle both classes (`gate.approve` flips `character.md` and marks the asset accepted in\n`manifest.json`) — see `docs/plans/archive/command-undo-redo.md`.", "labels": ["P10", "P3", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/commands.test.ts", "before": "template is not a document, but it *is* an authored file inside the snapshot's pathspec, so\nundo restores it exactly the way it restores a scene.", "after": "template is not a document, but it is an authored file inside the snapshot's pathspec, so undo\nrestores it exactly the way it restores a scene.", "labels": ["P10", "P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/commands.test.ts", "before": "A check is a precondition on an *act* — something with a cost that running it would incur.", "after": "A check is a precondition on an act — something with a cost that running it would incur.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/commands.test.ts", "before": "stops are the converse: they interrupt an act rather than performing one, so there *is* state\nto read — whether a run or a turn is in progress — and the answer is what greys the Stop\nbutton and says why.", "after": "stops are the converse: they interrupt an act rather than performing one, so there is state to\nread — whether a run or a turn is in progress — and the answer is what greys the Stop button\nand says why.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/tests/gate.test.ts", "before": "editor's `RESOLVE →` opens the form: both open it on a *character* and leave which portrait\nto the author, so the form's first verdict is always a refusal about the empty `hash`.", "after": "editor's `RESOLVE →` opens the form: both open it on a character and leave which portrait to\nthe author, so the form's first verdict is always a refusal about the empty `hash`.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/upload.ts", "before": "A separate namespace from `asset.upload` on purpose — that brings an *image* into the\ncontent-addressed store, this copies a *document* into `archive/` and hands the conversation a\nquestion. Different noun, different destination, and one word between them.", "after": "A separate namespace from `asset.upload` on purpose. `asset.upload` brings an image into the\ncontent-addressed store; this copies a document into `archive/` and hands the conversation a\nquestion about it.", "labels": ["P10", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/upload.ts", "before": "It copies bytes into the repo from paths the author named — the bar `asset.upload` clears.", "after": "Confirmed because it copies bytes into the repo from paths the author named, which is the same\nbar `asset.upload` meets.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/view.ts", "before": "They address **editors**, not rooms. The shell is a mesh of panes, so what an author (or the\nagent driving for one) wants to say is \"show me the coverage strip\", optionally \"beside the\nscript\" — `view.open`, `view.focus`, and the two layout verbs. The names come from", "after": "They address editors, not rooms. The shell is a mesh of panes, so `view.open`, `view.focus` and\nthe two layout verbs name an editor and optionally where to put it. The names come from", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/view.ts", "before": "Which template *this* window is showing, remembered beside its live mesh.", "after": "Which template the asking window is showing, remembered beside its live mesh.", "labels": ["P10", "P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/view.ts", "before": "Not a module constant any more, and the reason it could never stay one is that it failed\nloudly: `view.applyLayout` in window A wrote the flat `pathux.template`, and\n`view.resetLayout` in window B then re-applied **A's** template to B. So it is derived from\nwho asked, like any other targeted act - and from the workspace, because `session.json` is\ninstall-global and two instances on two repos would otherwise share one window 0.", "after": "The key is derived from who asked rather than being a module constant: with a flat\n`pathux.template`, `view.applyLayout` in window A wrote it and `view.resetLayout` in window B\nthen re-applied A's template to B. It is also derived from the workspace, because\n`session.json` is install-global and two instances on two repos would otherwise share one\nwindow 0.", "labels": ["P10", "P9"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/view.ts", "before": "A command with no origin - the agent, CDP, main itself - remembers about the window its\neffect will actually land in, which is the focused one.", "after": "A command with no origin (the agent, CDP, main itself) is recorded against the window its\neffect will land in, which is the focused one.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/view.ts", "before": "`window` is not something a mesh can do to itself, so it is answered here instead of\npushed: the new renderer opens showing the editor, which is the same outcome by the only\nroute that exists.", "after": "A mesh cannot open a window for itself, so this case is answered in main rather than\npushed as an effect; the new renderer opens showing the editor", "labels": ["P12"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/view.ts", "before": "Whatever was on screen, put it back — a reset that leaves the window showing an\narrangement no file describes any more would be the one act that lies about itself.", "after": "Re-apply the arrangement that was on screen, so the window is never left showing an\narrangement no file describes any more", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/window.ts", "before": "Windows as commands. A window is a *renderer*, not a document: it holds a mesh of panes and", "after": "Windows as commands. A window is a renderer, not a document: it holds a mesh of panes and", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/window.ts", "before": "Everything a window remembers — its mesh, its selection, its layout template — is keyed by the\nindex main hands it, so closing one and opening another is genuinely a new window rather than\nthe old one wearing its state.", "after": "Everything a window remembers (its mesh, its selection, its layout template) is keyed by the\nindex main hands it.", "labels": ["P4", "P13"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/window.ts", "before": "No origin means the agent or CDP asked, and an unaddressed act lands where an unaddressed\neffect would — the focused window.", "after": "No origin means the agent or CDP asked, so the focused window is closed, which is where an\nunaddressed effect lands too", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/workspace.ts", "before": "No mutator here is `undoable`. `workspace.import` restructures the whole worktree, which is what\na shadow snapshot is worst at, and the `.imported` rename it leaves behind is a reversal the\nauthor can perform themselves; `workspace.reindex` writes one derived file, and undoing it means\nrunning it again; and `workspace.open`/`pick`/`create` write into a *different* tree than the\none the undo journal snapshots, so a shadow ref in the old repo could not restore it anyway.", "after": "No mutator here is `undoable`. `workspace.import` restructures the whole worktree, which a\nshadow snapshot handles worst, and the `.imported` rename it leaves behind is a reversal the\nauthor can perform themselves. `workspace.reindex` writes one derived file, and undoing it means\nrunning it again. `workspace.open`, `pick` and `create` write into a different tree than the one\nthe undo journal snapshots, so a shadow ref in the old repo could not restore it anyway.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/workspace.ts", "before": "One instance per workspace: the undo shadow refs, the committer and the agent conversation\nare all per project, and two live stacks over one repo overwrite each other's snapshots in\nsilence. So opening a project twice is refused, and running it anyway focuses the instance\nthat has it rather than switching. This can race `run`, which is why `run` re-decides.", "after": "One instance per workspace, because two live stacks over one repo silently overwrite each\nother's undo snapshots. Opening a project a second time focuses the instance that already has\nit rather than switching, and `run` re-decides because this check can race `run`.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/workspace.ts", "before": "Every sentence names the **resolved** root, so an author with `newFolder` checked reads where", "after": "Every sentence names the resolved root, so an author with `newFolder` checked reads where", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/workspace.ts", "before": "The dialog is not a permission: a folder the command would refuse is refused here too.", "after": "A folder chosen in the dialog still gets the refusal `workspace.open` would have given it", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/commands/workspace.ts", "before": "Pruned here rather than at the menu: this half can stat, and a list that heals as it is\nread is one the ten slots stay spent on projects that are actually there.", "after": "Pruned here rather than at the menu because main can stat the paths, which keeps the\nremembered slots spent on projects that still exist", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctor.ts", "before": "Without it a packaged app does not degrade, it fails at the first save — so it is better said\nbefore a workspace opens than discovered later in a dialog nobody can act on. It is\ndeliberately **not** fatal: someone who only wants to watch a generated VN should not need git\nto do it.", "after": "Without git a packaged app fails at the first save rather than degrading, so the check runs\nbefore a workspace opens. A missing git is deliberately not fatal: watching a generated VN\nneeds no git.", "labels": ["P10", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctor.ts", "before": "We do not bundle a portable git. It would add tens of megabytes and a second thing to keep\npatched, to solve a problem only Windows has — where the answer is a link to the installer.", "after": "A portable git is not bundled. It would add tens of megabytes and a second thing to keep\npatched, to solve a problem only Windows has, where the answer is a link to the installer.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctor.ts", "before": "Where a stranger goes to fix it. Named once, so the dialog and the note cannot disagree. */", "after": "The download page offered when git is missing. Named once so the dialog and the note agree. */", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctor.ts", "before": "What the author is told, in one sentence, wherever they are told it. */", "after": "The one sentence shown wherever a missing git is reported to the author. */", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctor.ts", "before": "The number out of `git version 2.45.1.windows.1`. Absent is not a failure — a git that ran is\na git that works, and the string is only ever shown to a human.", "after": "The number out of `git version 2.45.1.windows.1`. A missing number is not a failure: git ran,\nand the string is only shown to a human.", "labels": ["P9"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctor.ts", "before": "The finding, once, for the code that must decide whether to spawn git at all. Recording it is a\nseparate call from making it so `checkGit` stays pure — a test that injects a probe would\notherwise leave its answer behind for every test after it.", "after": "The startup finding, for code deciding whether to spawn git at all. Recording it is a separate\ncall from making it so that `checkGit` stays pure; a test that injects a probe would otherwise\nleave its answer behind for every test after it.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctor.ts", "before": "It reads `ok` until told otherwise, because everything that asks runs after startup did.", "after": "The default is `ok`, because everything that asks runs after startup has recorded a finding.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "Most children a branch prints before it says how many it dropped. */", "after": "Most children a branch prints before the remainder is folded into a counted `more` node. */", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "Every picture the project implies, from `buildSlotGraph`. **Optional**: without it the tree is\nexactly what it was, and the Unapproved root is simply absent rather than empty — a caller that\nhas not built the graph must not be made to claim nothing is waiting.", "after": "Every picture the project implies, from `buildSlotGraph`. Optional: when it is absent the\nUnapproved root is left out rather than drawn empty, so a caller that has not built the graph\nnever claims nothing is waiting.", "labels": ["P10", "P5"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "The project's skills, if the caller looked for them. **Optional, and `[]` is not the same as\nabsent**: undefined leaves the branch out entirely (which is what every caller that knows\nnothing about skills wants), while an empty array draws an empty heading on purpose — see", "after": "The project's skills, if the caller looked for them. An empty array is not the same as absent:\nundefined leaves the branch out entirely (which is what a caller that knows nothing about\nskills wants), while an empty array draws an empty heading on purpose — see", "labels": ["P10", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "One skill, as the tree needs it. Deliberately **not** `@vn/authoring`'s `Skill`, which carries\nthe whole instruction body and absolute paths on disk — neither belongs on the wire, and a tree\nthat shipped the body would put every playbook in the renderer on every read.", "after": "One skill, as the tree needs it. Deliberately not `@vn/authoring`'s `Skill`, which carries the\nwhole instruction body and absolute paths on disk; neither belongs on the wire, and a tree that\nshipped the body would put every playbook in the renderer on every read.", "labels": ["P10", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "The remainder rides *under* that node rather than being dropped: the cap is about what a tree\ndraws at rest, not about what it is allowed to know, and an author who wants the other three\nhundred scenes has nowhere else to ask. The renderer expands it in place — a `more` node is a\ncontinuation of the list it ends, not a container — so paying for it is one click, and the\nshape stays a shape a caller that never expands anything can ignore.", "after": "The remainder becomes that node's children rather than being dropped, so the cap limits what is\ndrawn at rest rather than what the tree carries. The renderer expands a `more` node in place as\na continuation of the list it ends, and a caller that never expands anything can ignore it.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "Unreadable outranks unreachable: one is a fact about the story, the other about the disk,\nand the disk is the one somebody has to go fix.", "after": "An unreadable storyboard outranks an unreachable scene, because a disk problem is the one\nsomebody has to go fix", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "The skills, as leaves. **No file children**: the doc tree is identity, not content\n(`docs/reference/document-tree.md`), and a skill is one thing with an id, a name and a description — the\nsame granularity as a character. What is *inside* the directory is the Skills pane's own tree.", "after": "The skills, as leaves with no file children: the doc tree carries identity rather than content\n(`docs/reference/document-tree.md`), so a skill is one row with an id, a name and a description,\nthe same granularity as a character. The directory's contents are the Skills pane's own tree.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "**Drawn even when empty**, which no other branch is. A skill has to be findable before one\nexists, and the heading's own right-click menu is the only always-reachable way to make the\nfirst: `skeleton()` writes no `.aiagent/` at all, so every project created in the app starts\nhere, and an absent branch would hide the feature from exactly the authors who have not read\n`docs/`. The caller still decides — `input.skills` undefined means it did not look.", "after": "This branch is drawn even when empty, which no other branch is. A skill has to be findable\nbefore one exists, and the heading's right-click menu is the only reliably reachable way to make\nthe first one: `skeleton()` writes no `.aiagent/`, so every project created in the app starts\nwith none. An undefined `input.skills` still means the caller did not look, and leaves the\nbranch out.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "A skill with no description still hovers: the sentence says what the row *is*, which is\nthe one thing an author looking at an unfamiliar playbook needs before opening it.", "after": "A skill with no description still gets a tooltip, so an author looking at an unfamiliar\nplaybook is told what the row is before opening it", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "The frames whose scene has moved on since they were drawn. Accepting a frame says \"this is the\npicture I want\"; drift says the words it illustrates have changed underneath it — so a drifted\nframe is still the accepted one and is no longer a true `accepted` badge. Re-derived here on\nevery read, like everywhere else drift is asked about; never a stored flag.", "after": "The frames whose scene has moved on since they were drawn. Acceptance records that the picture\nis wanted; drift records that the prose it illustrates has changed since, so a drifted frame is\nstill accepted but no longer earns the `accepted` badge. Drift is re-derived on every read here\nas everywhere else, never stored as a flag.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "The manifest, filed the way it was *made*: one row per **slot** — the address a picture has,\n`aiko portrait`, `cafe — night plate` — showing what fills that slot now, with every earlier take\nof the same slot folded underneath it. A slot rendered eight times is one row and not eight,\nwhich is the difference between a list an author reads and a wall of near-identical thumbnails\ntold apart only by a hash.", "after": "The manifest, filed by slot: one row per slot address (`aiko portrait`, `cafe — night plate`)\nshowing what fills it now, with every earlier take of the same slot folded underneath. A slot\nrendered eight times is one row rather than eight near-identical thumbnails told apart by hash.", "labels": ["P13", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "slots, so its silence about a concept, an upload, a reference or a base-root asset is not a\nverdict — pruning on silence would bury every sketch in the project. With no graph passed at all\nthis is exactly the flat list it always was.", "after": "slots only, so its silence about a concept, an upload, a reference or a base-root asset says\nnothing about them, and pruning on that silence would bury every sketch in the project. Without a\ngraph this produces a flat list.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "Only takes no earlier slot spoke for: one picture can satisfy two slots, and filing it twice\nwould make one render read as two.", "after": "Only takes that no earlier slot claimed: one picture can satisfy two slots, and filing it\ntwice would make one render read as two", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "What fills the slot: what it resolved to, else the newest take. `pick` declines whenever the\nanswer is not certain, and a row still has to open on something — which is not a verdict on\nthe drafts, because choosing between them happens in the Unapproved branch, where all of\nthem are still listed one per row.", "after": "The slot shows what it resolved to, or the newest take when `pick` declined for want of\ncertainty. That choice does not judge the drafts: choosing between them happens in the\nUnapproved branch, which still lists all of them one per row.", "labels": ["P4", "P7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "one slot rendered nine times is one thing to look at, and saying nine would be a count of\nsomething the branch does not draw.", "after": "one slot rendered nine times counts as one, since the other eight are not drawn", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "A second index into nodes that already exist rather than a new kind of thing: an *Awaiting\napproval* row reuses the `asset:<hash>` id the Assets branch uses, so selection, routing and the\nright-click menu all work here with no renderer change. Only *Not yet rendered* is new, because", "after": "This branch indexes nodes that already exist rather than introducing new ones: an \"Awaiting\napproval\" row reuses the `asset:<hash>` id the Assets branch uses, so selection, routing and the\nright-click menu all work here with no renderer change. Only \"Not yet rendered\" is new, because", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "Zero candidates, deliberately — not `hash === undefined`. `pick` declines whenever the answer\nis not certain, so a slot holding three drafts resolves to nothing and would read as\nunrendered; those three drafts are what the other group is listing.", "after": "The test is zero candidates rather than `hash === undefined`, because `pick` declines for\nwant of certainty: a slot holding three drafts resolves to nothing and would read as\nunrendered, while those three drafts are what the other group lists", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "What a backlink entry is *about*. One of the three things a document in this project can be. */", "after": "What a backlink entry is about: one of the three things a document in this project can be. */", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "Only a scene asks which shot: for a character the same frame can satisfy several, and the\nstrip that groups by shot is the scene's.", "after": "The shot is recorded only for a scene: for a character the same frame can satisfy several,\nand the strip that groups by shot is the scene's", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "The bible link is the sheet's own path when the sheet lives under wiki/. Which *other* notes\nmention it is `bible.search` — ranked and budgeted — not a precomputed index.", "after": "The bible link is the sheet's own path when the sheet lives under wiki/. Finding the other\nnotes that mention it is `bible.search`, which is ranked and budgeted, not an index built here", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "Immediately before Assets: it is a lens on the same nodes, so it sits next to them, and the\nfour roots that were here keep the order the sidebar has always drawn them in.", "after": "Placed immediately before Assets because it is a second view of the same nodes; the other\nroots keep the order the sidebar has always drawn them in", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "Skills sit with the other authored input, after Wiki — and before Unapproved, because nothing\nmay come between the two lenses on the manifest.", "after": "Skills sit with the other authored input, after Wiki and before Unapproved, so that nothing\ncomes between Unapproved and Assets", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "A scene is a subject too: what illustrates it is the same question asked of a different key,\nand it is the one that lets a pane showing prose show the frames drawn from it.", "after": "A scene is a subject too, backlinked under its own key, which is what lets a pane showing\nprose also show the frames drawn from that prose", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/doctree.ts", "before": "`base` is prepended to every id and path while the **structure** still comes from the paths as\ngiven. That is what lets a caller walk one subdirectory and still hand back rows a click can act\non: the ids come out `file:.aiagent/skills/<id>/SKILL.md`, which is the workspace-relative path", "after": "`base` is prepended to every id and path while the structure still comes from the paths as\ngiven. That lets a caller walk one subdirectory and still hand back rows a click can act on: the\nids come out `file:.aiagent/skills/<id>/SKILL.md`, which is the workspace-relative path", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "Every window onto this workspace. A window is a *view*: one process, one session, one command", "after": "Every window onto this workspace. A window is a view: one process, one session, one command", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "One window closed. Only the turns parked on *that* window end; the others are still asked. */", "after": "One window closed. Only the turns parked on that window end; the others are still asked. */", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "Resolve the workspace once, before anything can ask for it: `--project` (or `VN_PROJECT`),\nthen the most recent project that still exists, then the picker, then the seeded sample.", "after": "Resolve the workspace once, before anything can ask for it. `--project` (or `VN_PROJECT`) wins\nwhen it is given. Otherwise the most recent project that still exists is opened, failing that\nthe picker is shown, and the seeded sample is the last resort.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "window switcher see only this — and three windows all called `vnstudio` cannot be told apart.", "after": "window switcher show only the window title, and three windows all called `vnstudio` cannot be\ntold apart.", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "Acquire the new root *before* releasing the old one, so a switch never drops a lock it might", "after": "Acquire the new root before releasing the old one, so a switch never drops a lock it might", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "A repo appears here only when the directory *is* its root. A project opened inside a larger", "after": "A repo appears here only when the directory is its own root. A project opened inside a larger", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "its own event — a CLI run, another editor. That is what establishes the invariant every\nlater commit relies on: the app opens on a clean worktree, and every act ends with one.", "after": "its own event — a CLI run, another editor. Recording those changes first establishes the\ninvariant every later commit relies on: the app opens on a clean worktree, and every act ends\nwith one.", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "The dialog, once, before a window exists — so the first thing a stranger sees on a machine", "after": "Show the dialog once, before a window exists, so the first thing a stranger sees on a machine", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "A brand-new install looks completely healthy — it opens, it draws a tree, the palette is full —\nright up to the first run, which fails somewhere deep in a task with a message about a provider.\nThis notice states the same fact earlier and more plainly, and links the pane that fixes it.", "after": "A brand-new install looks healthy right up to the first run, which fails somewhere deep in a\ntask with a message about a provider. This notice states the same fact earlier and more plainly,\nand links the pane that fixes it.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "project, guarded by the notification log itself — nothing else remembers, and a second copy\nevery launch would be nagging rather than news. Under `--mock` there is nothing to warn about:\na mock run calls no provider, which is exactly the state someone trying the app out is in.", "after": "project, guarded by the notification log itself, because nothing else remembers. Under `--mock`\nthere is nothing to warn about, since a mock run calls no provider.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "Broadcast, in full: the note frame is per-window chrome, so an author looking at the other\nmonitor should still see what happened, and every window's bell count must stay current.", "after": "Every window gets the whole note. The note frame is per-window chrome, so an author looking at\nthe other monitor should still see what happened, and every window's bell count stays current.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "Ask the window that started the turn, and **focus it**: `agent:event` broadcasts, so every", "after": "Ask the window that started the turn, and focus it. `agent:event` broadcasts, so every", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "Targeted: a `view.*` effect means *here*, in the window whose palette or menu ran the\ncommand. `windowFor` falls back to the focused window for the agent, CDP and main.", "after": "A `view.*` effect is targeted at the window whose palette or menu ran the command.\n`windowFor` falls back to the focused window for the agent, CDP and main.", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "The revision is what tells the renderer \"files moved and you did not move them\". An\nundo/redo always qualifies; so does any mutating command that did not come from the\nrenderer's own `exec` — the agent, CDP, or main itself — because for those nothing on\nthat side invalidated, and the document tree would sit on a project that has changed.\nA `ui` one is deliberately left out: `exec` already invalidated, and this would count\nit twice.", "after": "The revision tells the renderer that files moved without it moving them. An undo or redo\nalways counts, as does a mutating command from the agent, CDP or main, whose changes the\nrenderer never invalidated. A `ui` command is left out because `exec` already invalidated.", "labels": ["P9", "P12"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "**Both roots**, in the order `AssetStore` reads them: base art — portraits, model sheets,\nlocation plates — lives beside the inputs at `assets/objects/`, and only shot frames are", "after": "Both roots are searched, in the order `AssetStore` reads them: base art (portraits, model\nsheets, location plates) lives beside the inputs at `assets/objects/`, and only shot frames are", "labels": ["P13", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "Only *this* window's requests. With one window \"that window closed\" and \"there is nobody\nleft\" were the same fact; with four, ending every parked turn would be a bug.", "after": "Ends only this window's requests. With four windows open, ending every parked turn because\none closed would be a bug.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "cancels such a close outright unless this event is answered (the window could not be closed\nat all); `preventDefault` here means \"unload anyway\". Asked once per window, including\nduring the cascade a quit produces.", "after": "cancels the close outright unless this event is answered; `preventDefault` here means \"unload\nanyway\". Asked once per window, including during the cascade a quit produces.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/index.ts", "before": "Exit **before creating any window**, so the author sees the existing instance come forward", "after": "Exit before creating any window, so the author sees the existing instance come forward", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/instancelock.ts", "before": "One app instance per **workspace**, not per install.", "after": "One app instance per workspace, not per install.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/instancelock.ts", "before": "conversation are all per project. Two instances on the *same* repo collide in every one of\nthem — and silently, because each stack's `seq` starts at zero, so instance B's first command\noverwrites the snapshot instance A's first command is holding. That is why the lock is keyed\nby the resolved root and why `app.requestSingleInstanceLock()` — a global lock — is\ndeliberately not used: it would buy the safety by forbidding something that was never\ndangerous.", "after": "conversation are all per project. Two instances on the same repo collide in every one of\nthem, and silently, because each stack's `seq` starts at zero, so instance B's first command\noverwrites the snapshot instance A's first command is holding. The lock is therefore keyed by\nthe resolved root. `app.requestSingleInstanceLock()` is a global lock and is deliberately not\nused, because it would also forbid two instances on two different repos.", "labels": ["P13", "P10", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/instancelock.ts", "before": "**The lock is a listening socket, not a file.** Binding *is* acquiring: it succeeds for\nexactly one process, and the endpoint dies with the process, so there is no stale-pid\nbookkeeping and no lock that outlives a crash. On Windows a named pipe leaves nothing behind\nat all; on posix a socket file survives a hard kill, so `EADDRINUSE` is followed by a connect\nattempt — a refused connect means the owner is gone, and the file is unlinked and the listen\nretried once.", "after": "The lock is a listening socket rather than a file, so binding it is acquiring it: the bind\nsucceeds for exactly one process, and the endpoint dies with the process, so there is no\nstale-pid bookkeeping and no lock that outlives a crash. On Windows a named pipe leaves\nnothing behind. On posix a socket file survives a hard kill, so `EADDRINUSE` is followed by a\nconnect attempt: a refused connect means the owner is gone, so the file is unlinked and the\nlisten retried once.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/instancelock.ts", "before": "`platform` decides which rules to resolve *under*, so the resolver is picked explicitly rather\nthan taken from `node:path`'s default export: a bare `resolve` follows the host, which makes\n`lockAddress(root, 'win32')` mean something different on a Linux runner than on Windows — it\nleaves the backslashes in `C:\\DEV\\Story` as ordinary characters and prepends the cwd, so the\ntwo spellings of one directory stop agreeing.", "after": "`platform` decides which rules the path is resolved under, so the resolver is picked\nexplicitly rather than taken from `node:path`'s default export. A bare `resolve` follows the\nhost, so `lockAddress(root, 'win32')` would mean something different on a Linux runner than on\nWindows: it leaves the backslashes in `C:\\DEV\\Story` as ordinary characters and prepends the\ncwd, so the two spellings of one directory stop agreeing.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/instancelock.ts", "before": "Injected so a test can drive the socket layer without one. */", "after": "The socket calls this module makes, injected so a test can replace them with fakes. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/instancelock.ts", "before": "Taken — or a stale socket file left by a killed owner. Only the connect tells them apart,\nand only on posix: a Windows pipe cannot outlive the process that made it.", "after": "The address is either held by a live owner or a stale socket file left by a killed one.\nOnly a connect attempt tells the two apart, and only on posix, because a Windows pipe\ncannot outlive the process that made it.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/instancelock.ts", "before": "Is `root` open in another instance right now? A report about this instant — the caller must\nstill acquire for real, because the answer can change under it. Used by `workspace.open`'s\ncheck, which is allowed to race precisely because `run` re-decides.", "after": "Whether `root` is open in another instance at this instant. The caller must still acquire for\nreal, because the answer can change before it does. Used by `workspace.open`'s check, which is\nallowed to race because `run` re-decides.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/keyaudit.ts", "before": "Tier 2 of [`docs/plans/archive/auditing-the-api-key-instructions.md`]: whether the *words* in\n`docs/guides/api-keys.md` still describe what a vendor's own page says today.", "after": "Tier 2 of [`docs/plans/archive/auditing-the-api-key-instructions.md`]: whether the wording in\n`docs/guides/api-keys.md` still describes what a vendor's own page says today.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/keyaudit.ts", "before": "The pure half lives here so it can be tested without a network or a key: fetching and the model\ncall are in `scripts/audit-key-instructions.mjs`, and everything that decides anything is a\nfunction below. Nothing here writes: the audit's one hard rule is that `docs/guides/api-keys.md` is", "after": "The pure half lives here so it can be tested without a network or a key. Fetching and the model\ncall are in `scripts/audit-key-instructions.mjs`, and every decision is made by a function\nbelow. Nothing here writes. The audit's one hard rule is that `docs/guides/api-keys.md` is", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/keyaudit.ts", "before": "This file is deliberately not imported by `index.ts`. It is the desktop app's key guide it\nreasons about, which is why it lives here, but the app never runs it and it is not in the\nbundle.", "after": "This file is deliberately not imported by `index.ts`. It reasons about the desktop app's key\nguide, which is why it lives here, but the app never runs it and it is not in the bundle.", "labels": ["C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/keyaudit.ts", "before": "rendering or a bot wall is a thing this could not read, and reporting \"agree\" about it would\nbe a lie that reads exactly like a pass.", "after": "rendering or a bot wall cannot be read at all, and reporting \"agree\" about such a page would\nread exactly like a pass while being false.", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/keyaudit.ts", "before": "An inline tag becomes a space, so `<b>Create key</b>.` would otherwise read as\n\"Create key .\" — a difference the model would have to spend attention deciding to ignore.", "after": "An inline tag becomes a space, so without this `<b>Create key</b>.` would read as\n\"Create key .\" and the model would have to decide to ignore the gap", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/keyaudit.ts", "before": "model handed that will confidently report that our instructions no longer match — comparing\nthem, in effect, against nothing. The floor is what separates \"the vendor rewrote the page\"\nfrom \"we never saw the page\".", "after": "model handed that will confidently report that our instructions no longer match, having\ncompared them against nothing. The floor distinguishes a page the vendor rewrote from a page\nthat was never read.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/keyaudit.ts", "before": "Generous enough to hold a whole walkthrough, small enough that a weekly run stays cents.", "after": "The cap holds a whole walkthrough while keeping a weekly run's cost to cents.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/keyaudit.ts", "before": "The first line is the whole report for anyone reading a notification, so it must never be\nable to say \"nothing to do\" about a week when nothing was actually looked at. A run where\nevery page failed to load is the one that most looks like a pass and least is one.", "after": "The first line is the whole report for anyone reading a notification, so it must never say\n\"nothing to do\" about a week when nothing was read. A run where every page failed to load\nlooks most like a pass, so it gets a headline of its own.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/layouts.ts", "before": "because the renderer needs it too; this half is the I/O, so the commands stay the thin\nwrappers over it that the rest of `commands/` is.", "after": "because the renderer needs it too. This half is the I/O, so the commands over it stay as thin\nas the rest of `commands/`.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/layouts.ts", "before": "Cheap and stable: same bytes, same fingerprint, on any machine. */", "after": "The same bytes give the same fingerprint on any machine. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/layouts.ts", "before": "The porcelain codes that mean a merge left this path unresolved. Pure so the merge policy is\ntestable without a repo — `git status` gives these two characters and nothing else says it.", "after": "Whether this pair of porcelain status codes means a merge left the path unresolved. Pure so\nthe merge policy is testable without a repo; `git status` is the only source of these codes.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/layouts.ts", "before": "with no file still appears — the recipe answers for it — so the feature works on day one in\na project that predates it.", "after": "with no file still appears, answered for by its built-in definition, so the feature works in a\nproject that predates it.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/layouts.ts", "before": "Put the shipped layouts back. `all` additionally deletes the author's own, which \"reset\" does\nnot promise on its own — hence two words rather than one flag.", "after": "Put the shipped layouts back. The `all` scope additionally deletes the author's own layouts,\nwhich \"reset\" alone does not promise, so the scope is a named value rather than a boolean flag.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/layouts.ts", "before": "Make sure the project tells git not to merge layouts. Appends: a `.gitattributes` an author\nwrote is theirs, and this adds only the one rule it needs — not this repo's own\n`* text=auto eol=lf`, for the reason `ensureGitAttributes` gives.", "after": "Make sure the project tells git not to merge layouts. Appends rather than replaces, because a\n`.gitattributes` an author wrote is theirs. Only the one rule needed is added, not this repo's\nown `* text=auto eol=lf` (for the reason `ensureGitAttributes` gives).", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "notification is not one of a *project's authored files*, so it does not belong in the package\nthat models those — even though `vngen/state/` is where this project keeps its append-only logs", "after": "notification is not one of a project's authored files, so it does not belong in the package\nthat models those, even though `vngen/state/` is where this project keeps its append-only logs", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "1. **Flags are patched in place.** `\"r\"` (read) and `\"h\"` (hidden) are single ASCII digits in", "after": "1. Flags are patched in place. `\"r\"` (read) and `\"h\"` (hidden) are single ASCII digits in", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "2. **The file is union-merged** (`merge=union` in the project's `.gitattributes`), so a line", "after": "2. The file is union-merged (`merge=union` in the project's `.gitattributes`), so a line", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "Build a line. **The key order here is load-bearing**: `JSON.stringify` preserves insertion\norder, and `v`/`r`/`h` must land in the line's pure-ASCII head where `patchFlag` can find them\nahead of any authored text. A test pins it.", "after": "Build a line. The key order here is load-bearing: `JSON.stringify` preserves insertion order,\nand `v`/`r`/`h` must land in the line's pure-ASCII head where `patchFlag` can find them ahead\nof any authored text. A test pins that order.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "Where each line starts and ends, in **bytes**. Tolerates `\\r\\n` and a missing final newline. */", "after": "Where each line starts and ends, in bytes. Tolerates `\\r\\n` and a missing final newline. */", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "is skipped rather than thrown over — see `migrateNotification` for why that is the right trade\nhere. Missing file → `[]`.", "after": "is skipped rather than raising, for the reason `migrateNotification` gives. A missing file\nreads as an empty list.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "**Byte offsets, never character offsets.** Decoding the file to a string first would work on\nthe patched line — its head is ASCII — and be wrong about *where that line begins*: a string\nindex runs short by one for every extra byte of every multi-byte character above it, and this\napp's own messages are full of them (`⟲`, `…`, em-dashes). One such character in an earlier\nline and the write lands inside somebody else's message.", "after": "The offsets are byte offsets, never character offsets. Decoding the file to a string first\nwould work on the patched line, whose head is ASCII, but would place the start of that line\nwrongly: a string index runs short by one for every extra byte of every multi-byte character\nabove it, and this app's own messages are full of such characters (`⟲`, `…`, em-dashes). One\nof them in an earlier line and the write lands inside somebody else's message.", "labels": ["P13", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "The log of the project that is open **now**. Resolved per call and never captured: a captured\nroot would write into the directory `workspace.create` is about to check for emptiness, and\ncreating a project would start failing. `undefined` while no project is open.", "after": "The log of the project that is open at this moment. Resolved per call and never captured: a\ncaptured root would write into the directory `workspace.create` is about to check for\nemptiness, and creating a project would start failing. `undefined` while no project is open.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "File it, then show it. The event fires either way — a held note is still news. */", "after": "Write the note, then announce it. The event fires even when the note is only held. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "Announced, not just written: the dialog refetches after its own `notify.*` command, but the\npalette, the agent and CDP reach the same commands, and a bell left counting an already-read\nnotification is wrong however the flag moved.", "after": "Announces the change as well as writing it. The dialog refetches after its own `notify.*`\ncommand, but the palette, the agent and CDP reach the same commands, and a bell left counting\nan already-read notification is wrong.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/notifications.ts", "before": "Hide a set of ids in one act — what \"Clear\" is, over exactly what the dialog drew. */", "after": "Hide a set of ids in one act. \"Clear\" calls this with exactly the ids the dialog drew. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/pages.ts", "before": "Where the two files land, relative to the project root. Also what a caller reports as written. */", "after": "Where the workflow lands, relative to the project root. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/pages.ts", "before": "Where this build keeps its copy of the bundled renderer, in the order a build is likely to be\none of these. Mirrors `resources.ts`, and for the same reason: `__dirname` is\n`apps/desktop/dist/main` in a packaged build and `apps/desktop/src/main` under jest, and the\ncaller should not have to learn which kind of build it is running in.", "after": "Candidate paths for the bundled renderer, likeliest first. `resources.ts` resolves its files the\nsame way and for the same reason: `__dirname` is `apps/desktop/dist/main` in a packaged build and\n`apps/desktop/src/main` under jest, and the caller asks for the renderer by name rather than\nlearning which kind of build it is running in.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/pages.ts", "before": "The bundled renderer's path in this build, failing by name. A packaging mistake that drops it\nis invisible until someone runs the command, so the error says where it looked.", "after": "The bundled renderer's path in this build. A packaging mistake that drops it is invisible until\nsomeone runs the command, so the error says where it looked.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/pages.ts", "before": "Only first-party actions are used. The publish is a force-push of a single-commit orphan\nbranch: the published branch is a rendering rather than a history, and nothing downstream reads\nits past commits. The guard above the push refuses a branch this workflow did not write, so a\nhand-built `gh-pages` cannot be destroyed by installing this.", "after": "Only first-party actions are used. The publish is a force-push of a single-commit orphan branch;\nnothing downstream reads the published branch's past commits. The workflow refuses a publish\nbranch that carries no `.vn-pages` marker, so a hand-built `gh-pages` cannot be destroyed by\ninstalling this.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/pages.ts", "before": "The `.gitattributes` line the committed bundle needs, for the reason layout templates need\ntheirs: it is generated output, so a three-way merge of two builds' copies produces a file\nneither build wrote. Reinstalling is the resolution.", "after": "The `.gitattributes` line the committed bundle needs. It is generated output, so a three-way\nmerge of two builds' copies produces a file neither build wrote. Reinstall to resolve one.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/pages.ts", "before": "No .gitattributes yet. Writing one with just this line is correct.", "after": "no .gitattributes yet — writing one with just this line is correct", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/pages.ts", "before": "stamp, so an edited copy reads as stale too — which is the honest answer, since reinstalling\nwould overwrite the edit.", "after": "stamp, so an edited copy reads as stale too, because reinstalling would overwrite the edit.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/pages.ts", "before": "attribute. Returns the workspace-relative paths written, in the order a report should name them.", "after": "attribute. Returns the workspace-relative paths written: the workflow, the bundle, then\n`.gitattributes`.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/promptedit.ts", "before": "Pure: chunks and the current override in, the next override out — or the sentence refusing it.\nThe session owns loading and writing; this owns the rules, so `check` and `run` cannot disagree\nand a node test can drive every refusal without a project on disk.", "after": "The inputs are the chunks and the current override; the output is the next override, or the\nsentence refusing the edit. The session owns loading and writing and this file owns the rules, so\n`check` and `run` cannot disagree and a node test can drive every refusal without a project on\ndisk.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/promptedit.ts", "before": "One authorial act on a prompt. The commands' props, resolved to a shape with no strings left. */", "after": "One authorial act on a prompt. The commands resolve their props into this shape first. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/promptedit.ts", "before": "The pin and the binding are both resolved by the caller: a slot needs the manifest and the\ncycle check needs the whole project, and neither belongs in a pure rule.", "after": "The caller resolves both the pin and the binding. A slot needs the manifest and the cycle\ncheck needs the whole project, so neither belongs in a pure rule.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/promptedit.ts", "before": "A chunk key whose list went empty carries no meaning, and neither does a map of those.", "after": "A chunk key with an empty list carries no meaning, and neither does a map of empty lists.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/promptedit.ts", "before": "What the author's words are called on a card, for the sentence a refusal or a note prints. */", "after": "A chunk key as it is written into a refusal or a note. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/promptedit.ts", "before": "The same rule the drag gesture previews, so a drop and a typed command agree to the word.", "after": "Applies the same rule the drag gesture previews, so a drop and a typed command agree to\nthe word.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/promptedit.ts", "before": "The fingerprint is taken over the chunks *as the override leaves them*, so muting one\nafter condensing holds the result rather than silently sending a prompt it never saw.", "after": "The fingerprint covers the chunks as the override leaves them, so muting one after\ncondensing holds the result rather than sending a prompt the author never saw.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/promptedit.ts", "before": "Positional: `refs` is inside the task hash, so the same picture twice on one chunk is a\nsecond slot in the list rather than a duplicate that costs nothing.", "after": "`refs` is positional and sits inside the task hash, so the same picture twice on one\nchunk would be a second slot in the list rather than a duplicate that costs nothing.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/rename.ts", "before": "A rename changes **the name the document is drawn under**, and where that name lives depends on\nwhat the document is. A character or a location sheet is named by front-matter `name:` — the\nfield the model reads, wherever the sheet is filed. Everything else is named by its title, which\n`@vn/bible` reads as \"front-matter `title:`, else the first H1, else the filename stem\"; a rename\nwrites wherever the title was *read* from, or the tree would go on showing the old name and the\nrename would look like it failed.", "after": "A rename changes the name the document is drawn under, and where that name lives depends on what\nthe document is. A character or a location sheet is named by front-matter `name:` — the field\nthe model reads, wherever the sheet is filed. Everything else is named by its title, which\n`@vn/bible` takes from front-matter `title:`, then the first H1, then the filename stem. A\nrename writes wherever the title was read from, or the tree would go on showing the old name and\nthe rename would look like it failed.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/rename.ts", "before": "Pure, and tested as such: the write itself is `WorkspaceSession.renameDoc`, over the same\n`writeDocFile` every other document save goes through.", "after": "These functions are pure and are tested that way. The write itself is\n`WorkspaceSession.renameDoc`, over the same `writeDocFile` every other document save goes\nthrough.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/rename.ts", "before": "Rewrite a document so it is known by `name`. `path` is workspace-relative and decides only one\nthing — whether a conventional home makes this an entity sheet — with a `type:` tag as the\nfallback, the same way round `checkDocWrite` asks it.", "after": "Rewrite a document so it is known by `name`. `path` is workspace-relative and decides only\nwhether a conventional home makes this an entity sheet, falling back to a `type:` tag — the same\norder `checkDocWrite` asks in.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/rename.ts", "before": "Titled by its filename and nothing else, and the file does not move — so the name has to be\nwritten down somewhere, and a heading at the top is what a page with no title reads best as.", "after": "The page is titled by its filename and nothing else, and the file does not move, so the name\nhas to go into the text. A heading at the top is how a page with no title reads best.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/resources.ts", "before": "Files the *app* ships, as opposed to files the author wrote.", "after": "Files the app ships, as opposed to files the author wrote.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/resources.ts", "before": "There is exactly one so far — `docs/guides/api-keys.md`, which the Setup pane renders — and it has\nthe awkward property of living at the repo root in a checkout and inside the installer's", "after": "There is exactly one so far — `docs/guides/api-keys.md`, which the Setup pane renders — and it\nhas the awkward property of living at the repo root in a checkout and inside the installer's", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/resources.ts", "before": "Where a shipped file might be, in the order a build is likely to be one of these.", "after": "Candidate paths for a shipped file, in the order a build is likeliest to match.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/resources.ts", "before": "`process.resourcesPath` is Electron's and is undefined under plain node, which is what makes\nthis testable: a jest run falls through to the checkout, as does `pnpm dev`.", "after": "`process.resourcesPath` is Electron's and is undefined under plain node, so a jest run falls\nthrough to the checkout, and so does `pnpm dev`.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/resources.ts", "before": "The first candidate that exists, or `undefined` — never a path that is not there. */", "after": "The first candidate that exists, or `undefined` when none of them do. */", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/reviews.ts", "before": "renderer. A task that failed is exactly when the log is most likely to be ragged, so a\nmalformed entry drops rather than throwing — the inspector shows \"no critique recorded\".", "after": "renderer. The log is most likely to be malformed when a task has failed, so a malformed entry is\ndropped rather than throwing, and the inspector shows \"no critique recorded\".", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/reviews.ts", "before": "Keep only the entries that parse; a bad one costs its own review, not the attempt. */", "after": "Keep only the entries that parse, so a bad entry costs its own review rather than the attempt. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "the effect is pushed *while* a command is still running, which is exactly when that host\nhas not returned an outcome to attach anything to.", "after": "the effect is pushed while a command is still running, which is when that host has not\nreturned an outcome to attach anything to.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "scene's problem: with `reportBroken` it becomes a `null` the tree draws a badge for, and\nwithout it the scene is simply absent, which is what every other reader wants.", "after": "scene's problem. With `reportBroken` it becomes a `null` the tree draws a badge for. Without\nthat option the scene is simply absent, which is what every other reader wants.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The character edit that applies one rung's prompt override — {@link characterNotesEdit} with\nthe other field. Clearing an override must be stated explicitly: an empty override object is\nwhat `overrideData` serializes as a removed key, so `undefined` here still produces an edit.", "after": "The character edit that applies one rung's prompt override. Clearing an override must be\nstated explicitly: an empty override object is what `overrideData` serializes as a removed\nkey, so `undefined` here still produces an edit.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "names, and the question being asked is whether *this* report still says one.", "after": "names, and the question being asked is whether this report still says one.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "A plan and its verdict are the decisive turns of a conversation and neither reaches the\nloop's event stream as a transcript line — so both are recorded here, at the seam that\nasks. The renderer runs the same two reducers on its own copy.", "after": "A plan and its verdict are the decisive turns of a conversation, and neither reaches the\nloop's event stream as a transcript line, so both are recorded here. The renderer runs\nthe same two reducers on its own copy.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The author's answer is the one turn of theirs that never passes through `run`, and the\nloop does not emit it — so a transcript that did not record it here would lose it. The\nquestion goes down with it, options and all: \"the second one\" is unreadable without the\nlist it picked from. A declined confirmation needs no such care: that is a `blocked` event.", "after": "The author's answer never passes through `run` and the loop does not emit it, so only a\nrecord made here keeps it — question included, since \"the second one\" is unreadable\nwithout its list. A declined confirmation needs none of this: it is a `blocked` event.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The backend for the next turn. Native when the resolved chat backend can hold a conversation,\nwhich is the cached path; the text path otherwise.", "after": "The backend for the next turn. A chat backend that can hold a conversation gets the native\nbackend, which is the cached path. Every other backend gets the text path.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The capability `vnauthor` does not have: the same two calls `asset.regenerate` makes, so\nan agent-started re-render takes the busy flag a pipeline run takes.\nThe author's own words are the authority for an approval, so the seam carries the model\nthat reads them alongside the two acts it gates.", "after": "Approval is authorized by the author's own words, so this seam carries the model that\nreads them alongside the two acts it gates.", "labels": ["P4", "P12"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "answer buys a *grant* of attempts, and the loop spends them without asking again.", "after": "answer buys a grant of attempts, and the loop spends them without asking again.", "labels": ["P10", "P8"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "model swapped here is the one the next attempt is made against. One try on it, because a\nmodel the author picked failing is news, and worth telling them rather than grinding on.", "after": "model swapped here is the one the next attempt is made against. It gets one try, so a\nmodel the author picked that also fails is reported rather than retried repeatedly.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Rebuild that map. Throws if a file already sits at the path and the generator did not\nwrite it. */", "after": "Rebuild the generated project map. Throws if a file already sits at the path and the\ngenerator did not write it. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The project map is a file, and this session outlives every rewrite of it — including the\nagent's own `update_context`. Re-read it per turn so the map above the tool output is not\nan older, more authoritative-looking answer than the tools give. Section by section, so a\nrewritten map supersedes itself in a message rather than invalidating the cached prefix.", "after": "This session outlives every rewrite of the project map, the agent's own `update_context`\nincluded, so the map is re-read per turn and never outranks the tool output. Refreshed\nsection by section, so a rewrite supersedes itself rather than invalidating the cached\nprefix.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "calls `refreshSystem(systemSections(await loadContext(...)))` before every turn, so this *is*\nwhat the next turn sends — and it can be answered before an agent has ever been built, which\nis when an author most wants to check what it was told.", "after": "calls `refreshSystem(systemSections(await loadContext(...)))` before every turn, so this is\nexactly what the next turn sends — and it can be answered before an agent has ever been\nbuilt, which is when an author most wants to check what it was told.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "the commit that did — the pointer is about where the conversation *is*, not about who put\nit there.", "after": "the commit that did — the pointer records where the conversation is, not who put it there.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "A saved conversation, for reading. It ends the live one — the model is *not* shown what comes\nback, so leaving the previous turns in its context while the screen shows somebody else's\nwould be the one arrangement in which neither the author nor the agent knows what is being\ntalked about.", "after": "A saved conversation, for reading. It ends the live one: the model is never shown what comes\nback, so leaving the previous turns in its context while the screen shows another\nconversation would leave the author and the agent talking about different things.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Reopened on the binding it was had on. A conversation reads as the model that wrote it —\nits turns, its refusals, its reasoning — so answering the next question on whatever the\nlast conversation happened to leave bound is the one thing nobody means. `clearAgent` has\nalready closed the live thread, so nothing is written here: the next turn opens a thread\ncarrying this binding on its own line 0.", "after": "Reopened on the binding it was recorded with, because a conversation reads as the model\nthat wrote it. Nothing is written here — `clearAgent` has already closed the live thread,\nand the next turn opens a thread carrying this binding on its own line 0.", "labels": ["P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The model and effort one analysis runs at. An empty field means *whatever the agent is bound\nto* — so a scripted `report.agent(thread='t3')` does the sensible thing without naming a", "after": "The model and effort one analysis runs at. An empty field means whatever the agent is bound\nto, so a scripted `report.agent(thread='t3')` does the sensible thing without naming a", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The conversation an analysis would read: the one named, else the newest. */", "after": "The conversation an analysis would read. The one named, or the newest when none is named. */", "labels": ["P4", "P7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "disabled control shows verbatim, and the key one is keyed to the *chosen* model — switching", "after": "disabled control shows verbatim, and the key one is keyed to the chosen model — switching", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Archive the report, and shrug if that fails. The author has the analysis on screen either way;\nrefusing to show it because a copy could not be written would be losing the thing they paid a\nminute and a model call for over the thing they did not ask for.", "after": "Archive the report, and report no file when that fails. The author has the analysis on screen\neither way, and a copy they did not ask for is not worth withholding the analysis they paid a\nminute and a model call for.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Belt to `issueUrl`'s own brace: what reaches the shell is checked, not merely composed.", "after": "Checks the URL that reaches the shell rather than trusting what `issueUrl` composed", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "tree's *Awaiting approval* group is a projection of, so the agent and the tree can never", "after": "tree's “Awaiting approval” group is a projection of, so the agent and the tree can never", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Approve one of them through whichever door it belongs to. */", "after": "Approve one `Approvable` through whichever door it belongs to. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "*on* the agent, and running it on the model being checked would not be a check. Returns", "after": "on the agent, and running it on the model being checked would not be a check. Returns", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "refused by name before this could be reached, and a second sentence beside those reads as a\nsecond rule. Their prereqs are still listed — what a sketch was drawn from is worth showing\neven where there is nothing to approve.", "after": "refused by name already, and a second sentence beside those reads as a second rule. Their\nprereqs are still listed, because what a sketch was drawn from is worth showing regardless.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "and a pane offering to replace *that* would supersede a picture already moved past.", "after": "and a pane offering to replace a superseded render would supersede a picture already\nmoved past.", "labels": ["P10", "P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Accepting is the whole point of suspension: it says these bytes are the answer, and they\nwere drawn against a reference that has since moved.", "after": "Accepting says these bytes are the answer, and a suspended asset was drawn against a\nreference that has since moved.", "labels": ["P4", "P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "After suspension deliberately: that is a claim about *these* bytes resting on a reference\nthat moved, which is the more specific thing to say. This one is about other bytes.", "after": "Checked after suspension deliberately: suspension is a claim about these bytes resting on a\nreference that moved, which is more specific than a claim about other bytes upstream.", "labels": ["P10", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "snapshot to `tasks.jsonl` *is* the requeue — `loadGraph` replays last-writer-wins, which is\nhow `requeueFailed` already works — so this needs no new scheduler machinery.", "after": "snapshot to `tasks.jsonl` performs the requeue — `loadGraph` replays last-writer-wins, which\nis how `requeueFailed` already works — so this needs no new scheduler machinery.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Only a whole-prompt rewrite can lose a clause; in chunks mode the text *is* the chunks, and", "after": "Only a whole-prompt rewrite can lose a clause; in chunks mode the text is the chunks, and", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "gesture. A bare hash attaches with no `from` — an upload or a concept *is* its own identity, so\nthere is no slot under it and it can never drift.", "after": "gesture. A bare hash attaches with no `from`, because an upload or a concept carries its own\nidentity: there is no slot under it, so it can never drift.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Move a pinned reference to what its slot holds now. Written before the adoption is logged, and\nthe adoption is decided *before* either — so a refusal leaves the pin where it was rather than\na moved pin with no output.", "after": "Move a pinned reference to what its slot holds now. The adoption is decided first, then the\npin is written, then the adoption is logged — so a refusal leaves the pin where it was rather\nthan leaving a moved pin with no output.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Condense the chunks into one prompt and store it at the rung. The condensation is **held** the", "after": "Condense the chunks into one prompt and store it at the rung. The condensation is held the", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "At the project scope, `keys` is ignored *before* the write, because commit-on-save runs", "after": "At the project scope, `keys` is ignored before the write happens, because commit-on-save runs", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "**It never throws.** Every failure comes back as an `unreachable` verdict carrying its own", "after": "It never throws. Every failure comes back as an `unreachable` verdict carrying its own", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The model is one the *project* already configures for that vendor, not a name written down", "after": "The model is one the project already configures for that vendor, not a name written down", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The two writers are the same two `artNotesPlan` has — an entity sheet through `@vn/model`'s", "after": "The two writers are the same two `setArtNotes` has — an entity sheet through `@vn/model`'s", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Apply every refusal except `MOCK_PLACEHOLDER`: that one judges the bytes coming *in*, and", "after": "Apply every refusal except `MOCK_PLACEHOLDER`: that one judges the incoming bytes, and", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Every file under `.aiagent/skills`, as the Skills pane's own tree — the *content* the document", "after": "Every file under `.aiagent/skills`, as the Skills pane's own tree — the content the document", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Its own walk rather than a filter over `fileTree()`: that one is capped at 5000 files across\nthe whole project, so on a large one `.aiagent` could be truncated away and this pane would\ndraw an empty directory with nothing to say about why. It would also ship the entire project's\nfile list to paint a dozen rows.", "after": "Its own walk rather than a filter over `fileTree()`: that one is capped at `TREE_MAX_FILES`\nacross the whole project, so on a large one `.aiagent` could be truncated away and this pane\nwould draw an empty directory with nothing to say about why. It would also ship the entire\nproject's file list to paint a dozen rows.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "A skill is a directory with a `SKILL.md` in it. `writeFileAtomic` makes the directory, so\nnothing here has to — and the refusal that results is deliberately *not* `writeSkill`'s. That\none refuses an existing **directory**; this goes through `checkDocWrite` with an empty\n`seenHash` and refuses an existing **file**. So a directory a human has already put a vetted\n`run.mjs` in accepts the author's scaffold and rejects the agent's, which is the right way\nround: the human is the one who put the script there.", "after": "A skill is a directory with a `SKILL.md` in it, and `writeFileAtomic` makes the directory.\nThe refusal differs from `writeSkill`'s deliberately: that one refuses an existing\ndirectory, while this goes through `checkDocWrite` with an empty `seenHash` and refuses an\nexisting file. So a directory a human has already put a vetted `run.mjs` in accepts the\nauthor's scaffold and rejects the agent's, and the human is the one who put the script there.", "labels": ["P10", "C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Scaffold a character, a location, a wiki note or a skill from a **name**. The empty `seenHash` is what\nmakes this a creation: the write refuses over a file already there rather than overwriting\nwhatever the author had under that name.", "after": "Scaffold a character, a location, a wiki note or a skill from a name. The empty `seenHash` is\nwhat makes this a creation: the write refuses over a file already there rather than\noverwriting whatever the author had under that name.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "Rename one document in place. The **file never moves**: an id is derived from a name once, at", "after": "Rename one document in place. The file never moves: an id is derived from a name once, at", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "The decision behind `story.moveShot`, which is the one scene edit whose *rule* needs the", "after": "The decision behind `story.moveShot`, which is the one scene edit whose rule needs the", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "files and reports; the point is that a *later* insertion can no longer shift them.", "after": "files and reports; what it buys is that a later insertion can no longer shift them.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "equivalent. The screenplay is moved aside rather than deleted, and **last**: while it is still\na `.fountain` the project reports it on every load, so the rename is what finishes the import.", "after": "equivalent. The screenplay is moved aside rather than deleted, and moved last: while it is\nstill a `.fountain` the project reports it on every load, so the rename finishes the import.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "What a run would find. The count is a **dry run against mock providers** — what `vngen cost`", "after": "What a run would find. The count is a dry run against mock providers — what `vngen cost`", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/session.ts", "before": "files will not parse, which scenes name\na character the project does not have yet — and whether the *text* key resolves.", "after": "files will not parse, which scenes name a character the project does not have yet — and\nwhether the text key resolves.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/sessionstore.ts", "before": "Not to be confused with `WorkspaceSession` — that is the *backend* session (agent,\nproject model, pipeline). This is only remembered UI state, and it is reachable from a\ncommand as `ctx.host.state` for exactly that reason.", "after": "Not to be confused with `WorkspaceSession`, which is the backend session (agent, project\nmodel, pipeline). This is only remembered UI state, and it is reachable from a command as\n`ctx.host.state` for exactly that reason.", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/sessionstore.ts", "before": "Multiple app instances may hold the same file open, so writes are: taken under a", "after": "Multiple app instances may hold the same file open, so a write is taken under a", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/sessionstore.ts", "before": "Two instances setting *different* keys both survive; the same key is last-flush-wins.", "after": "Two instances setting different keys both survive; the same key is last-flush-wins.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/sessionstore.ts", "before": "It used to be `apps/desktop/.vndesktop`, derived from `__dirname` — which in a packaged app is\ninside `app.asar`, a *file*, so the store's `mkdir` failed `ENOTDIR` before the first window\nand the app hung with nothing on screen. A path relative to the bundle is a path relative to\na read-only archive, and no amount of care about the rest of packaging survives that.", "after": "It used to be `apps/desktop/.vndesktop`, derived from `__dirname`, which in a packaged app is\ninside `app.asar` — a file, so the store's `mkdir` failed with ENOTDIR before the first window\nand the app hung with nothing on screen. A path relative to the bundle is a path relative to a\nread-only archive.", "labels": ["P10", "P12"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/sessionstore.ts", "before": "`VN_DESKTOP_HOME` overrides it, which is what lets a test isolate the store; a development run\nshares the installed app's, because a second home is how a recents list quietly forks in two.", "after": "`VN_DESKTOP_HOME` overrides the location, which is how a test isolates the store. A development\nrun shares the installed app's directory, so the recents list does not fork in two.", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/sessionstore.ts", "before": "fires on every `set` whoever made it — the app hangs the renderer broadcast off it, so\na command that writes state moves the UI without having to remember to say so.", "after": "fires on every `set`, whatever made it. The app hangs the renderer broadcast off it, so a\ncommand that writes state moves the UI without having to announce the change itself.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/smoke.ts", "before": "`--smoke`: the one question a packaged build cannot answer by opening a window.", "after": "`--smoke` checks the one thing a packaged build cannot answer by opening a window: whether the\ntwo SDKs left out of the bundle still resolve.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/smoke.ts", "before": "`import()` at the moment a model is first called — so a packaging mistake that loses them (the\nlikely one being pnpm's symlink farm surviving into the app image) produces an installer that\nlaunches, opens a project, and throws `Cannot find module` the first time the agent is asked\nfor anything. Every check short of this one passes.", "after": "`import()` at the moment a model is first called. A packaging mistake that loses them (most\nlikely pnpm's symlink farm surviving into the app image) produces an installer that launches,\nopens a project, and throws `Cannot find module` the first time the agent is asked for\nanything. Every check short of this one passes.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/smoke.ts", "before": "So the packaged executable can be asked to do exactly that import and nothing else. It takes no\nkey, makes no call, and opens no window: constructing a client is a local act, and whether the\nkey it was handed is any good is `project.testKey`'s question, not this one.", "after": "The packaged executable does that import and nothing else. It takes no key, makes no call, and\nopens no window, because constructing a client is a local act. Whether the key it was handed is\nany good is `project.testKey`'s question, not this one.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/smoke.ts", "before": "constructor off the module — `backends/anthropic.ts` and `backends/gemini.ts`. The mirror is\ndeliberate rather than an import: those functions need a key and a model id to exist at all,\nand the failure under test is module resolution, which a constructor proves on its own.", "after": "constructor off the module — `backends/anthropic.ts` and `backends/gemini.ts`. Those functions\nneed a key and a model id to exist at all, so this mirrors them rather than importing them, and\nthe failure under test is module resolution, which constructing a client proves on its own.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/storygraph.ts", "before": "Edge ids are `<from>#choice:<index>` / `<from>#next`. Stable across reloads so a selection\nsurvives a rebuild, unique because a scene has one `next` and one choice per index, and\naddressable — the id carries exactly the arguments `story.removeChoice` / `story.setNext`\nneed, so a drag on a wire maps back to a command without a lookup table.", "after": "Edge ids are `<from>#choice:<index>` / `<from>#next`. They are stable across reloads, so a\nselection survives a rebuild, and unique because a scene has one `next` and one choice per\nindex. The id carries exactly the arguments `story.removeChoice` and `story.setNext` need, so\na drag on a wire maps back to a command without a lookup table.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctor.test.ts", "before": "is the whole reason the probe is a parameter.", "after": "is why the probe is a parameter.", "labels": ["P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctor.test.ts", "before": "Everything that asks runs after startup checked, so the default has to be the harmless one:\na `false` before the probe ran would put the app in read-only mode on a machine with git.", "after": "Callers run after startup has checked, so the default has to be the harmless one: reporting\n`false` before the probe ran would put the app in read-only mode on a machine that has git.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctor.test.ts", "before": "The separation exists for this: a checkGit in one test must not leave its answer behind.", "after": "`checkGit` and `noteGitHealth` are separate so that a `checkGit` call in one test does not\nleave its answer behind.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "No storyboard on disk is not the same as an empty one, and neither is a broken file.", "after": "A missing storyboard is different from an empty one, and different again from a file that\nwould not parse.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "Counted *and* carried: the cap is about what the tree draws at rest, not about what it is\nallowed to know, so the rest is one click away rather than gone.", "after": "The cap limits what the tree draws at rest rather than what it knows, so the dropped rows\nhang under the `more` node and stay one click away.", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "A caller that has not built the graph must not be made to claim nothing is waiting.", "after": "A caller that has not built the graph must not appear to claim that nothing is waiting.", "labels": ["P5"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "`a` is `accepted: true` and still waiting: the P3 gate owns a look, and nothing else does.", "after": "`a` is `accepted: true` and still waiting, because a portrait's approval comes from the P3\ngate and from nothing else.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "unrendered. Zero candidates is the predicate, which is what keeps the two groups disjoint.", "after": "unrendered. Having no candidates is the predicate instead, which keeps the two groups\ndisjoint.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "The one branch drawn empty. A skill has to be findable before one exists, so `[]` and\nundefined are deliberately different answers.", "after": "This is the one branch drawn empty. A skill has to be findable before one exists, so `[]` and\n`undefined` are deliberately different answers.", "labels": ["C7"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "No row hovers silently, and a skill with no description is exactly the row an author needs\ntold what it is.", "after": "Every row carries a tooltip, and a skill with no description is the row that most needs one.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "asks it and draws one row per *slot*, with the takes it moved on from folded underneath —\ndropped nowhere, because deleting one is a right-click on the row itself.", "after": "asks it and draws one row per slot, with the earlier takes folded underneath. Nothing is\ndropped, because deleting a take is a right-click on its own row.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "One row, because one slot — the heading counts what it draws, and two takes of one portrait\nare one thing to look at.", "after": "One row, because one slot. The heading counts the rows it draws, so two takes of one\nportrait count once.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "The row has to say that a second click is what opens that — nothing else in the tree\nbehaves this way, and a row that will not explain itself is the tooltip rule's own bug.", "after": "The row's note has to say that a second click opens the earlier takes, since nothing else\nin the tree behaves this way.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "the newest is what the row opens on — which is not a verdict: both candidates are still\nlisted one per row in the Unapproved branch, where the choosing actually happens.", "after": "the row opens on the newest. That is not a verdict: both candidates are still listed one\nper row in the Unapproved branch, where the choice is made.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "A scene is a subject too — the frames drawn from it are what a pane showing its prose puts\nunderneath, and the shot each one illustrates travels with it so they can be gathered by it.", "after": "A scene is a subject too. A pane showing its prose puts the frames drawn from it underneath,\nand each frame carries the shot it illustrates so the frames can be grouped by shot.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "The honest empty answer: no asset binds to a lore note, so it is in neither map and the pane\nshowing it says as much rather than drawing somebody else's art.", "after": "No asset binds to a lore note, so it is in neither map, and the pane showing it says so\nrather than drawing another subject's art.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "A caller that walked one subdirectory still hands back ids a click can act on: the prefix goes\non every id and path, while the *structure* comes from the paths as they arrived.", "after": "A caller that walked one subdirectory still gets ids a click can act on. The prefix goes on\nevery id and path, while the structure comes from the paths as they arrived.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/doctree.test.ts", "before": "The one thing the pure projection cannot check: `SkillEntry.file` is built by `relPath`, and\non Windows the path it starts from is backslash-separated. A backslash here would ship a path", "after": "The pure projection cannot check this. `SkillEntry.file` is built by `relPath`, and on\nWindows the path it starts from is backslash-separated; a backslash here would ship a path", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/instancelock.test.ts", "before": "Pinned, because the `platform` argument is a claim about which rules apply and not about\nwhich machine is asking: resolved under the host's rules instead, a Windows root read on a\nLinux runner keeps its backslashes and picks up that runner's cwd. The two assertions above\nagree with each other under either mistake, so only a constant catches it — and it catches it\non every machine rather than on the one that happens to disagree.", "after": "The `platform` argument says which rules apply, not which machine is asking: resolved under\nthe host's rules, a Windows root read on a Linux runner keeps its backslashes and picks up\nthat runner's cwd. Both assertions above pass under that mistake, so the value is pinned here.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/instancelock.test.ts", "before": "A second instance on the same project must not open — this is the whole point: two stacks\nwould overwrite each other's `refs/vn/undo/<seq>` snapshots in silence.", "after": "A second instance on the same project must not open: two stacks would silently overwrite\neach other's `refs/vn/undo/<seq>` snapshots.", "labels": ["P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/instancelock.test.ts", "before": "Nobody home is not an error: the caller may take the lock after all rather than exiting.", "after": "An unanswered address is not an error: the caller may take the lock rather than exiting.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/keyaudit.test.ts", "before": "The failure this exists for: a single-page app served to a bare fetch is a loading shell, and\na model handed that would report drift against, in effect, nothing.", "after": "This exists for one failure: a single-page app served to a bare fetch is a loading shell, and\na model handed that would report drift against nothing.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/keyaudit.test.ts", "before": "The report this exists to prevent: every page failed to load, and the top line reads as a\nclean week. A run that looked at nothing has to say so in its first sentence.", "after": "This prevents one report: every page failed to load and the top line reads as a clean week.\nA run that looked at nothing has to say so in its first sentence.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/keys.test.ts", "before": "string that was pasted, because the interesting failure here is not a wrong answer — it is a\nright answer with the secret in it.", "after": "string that was pasted, because the failure that matters here is a correct answer that carries\nthe secret in it.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/keys.test.ts", "before": "$VNAUTHOR_HOME is what jest already points somewhere empty; pointing it inside the project", "after": "jest already points $VNAUTHOR_HOME at an empty directory; pointing it inside the project", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/keys.test.ts", "before": "The refusal, not the call. `testKey` itself makes a real request and so is not something a test\nsuite may run; what is testable — and what the Setup pane's greyed-out button shows — is the\nsentence that says why there is nothing to try.", "after": "`testKey` itself makes a real request, so a test suite cannot run it. What is testable is the\nrefusal: the sentence that says why there is nothing to try, which is also what the Setup\npane's greyed-out button shows.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/keys.test.ts", "before": "And the call itself is the same answer, so a palette invocation cannot get further.", "after": "The call itself gives the same answer, so a palette invocation cannot get further.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/layouts.test.ts", "before": "Only the rule layouts need. This repo's own `* text=auto eol=lf` is a policy for this repo,\nnot one to install in somebody's project.", "after": "Writes only the rule layouts need. This repo's own `* text=auto eol=lf` is a policy for this\nrepo, not one to install in somebody's project.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/layouts.test.ts", "before": "The whole point of the `-merge` attribute, checked against real git rather than believed.", "after": "Checks the `-merge` attribute against real git rather than assuming it works.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/layouts.test.ts", "before": "...and the app says so rather than applying it.", "after": "The app reports the conflict rather than applying the layout.", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/layouts.test.ts", "before": "The sentence those two refusals tell the author to type, taken at its word.", "after": "Runs the commands those two refusals tell the author to type.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/notifications.test.ts", "before": "The landmine this whole module is shaped around: a string index into the file is NOT a byte\noffset once anything above the patched line is multi-byte, and this app's own messages are\nfull of `⟲`, `…` and em-dashes. So the non-ASCII goes *before* the line being patched, and\nthe assertion is on the neighbour as much as on the flag.", "after": "A string index into the file is not a byte offset once anything above the patched line is\nmulti-byte, and this app's own messages are full of `⟲`, `…` and em-dashes. The non-ASCII\ntherefore goes in the line ahead of the one being patched, and the neighbour is asserted too.", "labels": ["P4", "P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/pages.test.ts", "before": "The guard is only worth having if it precedes the destructive step.", "after": "The marker check has to come before the force push", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/promptedit.test.ts", "before": "The result's override, or a failure the test wanted to succeed. */", "after": "The result's override. Throws when the edit was refused. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/promptedit.test.ts", "before": "The mute is what makes the fingerprint differ from the unoverridden one; if it did not,\nmuting a chunk after condensing would silently send a prompt the model never saw.", "after": "The mute changes the fingerprint. Without that, muting a chunk after condensing would\nsilently send a prompt the model never saw.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/promptedit.test.ts", "before": "An upload names no slot, so it is a fixed point by construction — the refusal says so rather\nthan letting an author pin it somewhere it can never drift back from.", "after": "An upload names no slot, so it is a fixed point by construction. The refusal says so rather\nthan letting an author pin it somewhere it cannot drift back from.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/promptedit.test.ts", "before": "Dropping the last one must leave a sheet that looks untouched, not an empty block.", "after": "Dropping the last reference must leave no override at all, not an empty refs block", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/rename.test.ts", "before": "A rename is text in, text out — so where the name lives, and what is left untouched around it,\nare both testable without a workspace.", "after": "`renameInText` takes text and returns text, so where the name lives and what is left untouched\naround it are both testable without a workspace.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/rename.test.ts", "before": "The text a successful rename produced, so a case that refused fails loudly at the assertion. */", "after": "The renamed text. Throws when the rename was refused, so a refusing case fails loudly. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/rename.test.ts", "before": "Spliced, not re-serialized: the author ordered these keys and the body is untouched.", "after": "The front-matter is spliced rather than re-serialized, so the author's key order and the\nbody both survive", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/reviews.test.ts", "before": "The narrowing that lets the renderer treat `reviews` as `DefectReport[]`. Everything here\nis about the ragged case: `tasks.jsonl` is append-only JSON written across runs, so an\nattempt's reviews can be anything at all by the time the inspector reads them.", "after": "The narrowing that lets the renderer treat `reviews` as `DefectReport[]`. The cases here are\nmostly about ragged input: `tasks.jsonl` is append-only JSON written across runs, so an\nattempt's reviews may be any shape by the time the inspector reads them.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/sample.test.ts", "before": "The shipped template, through the path a first launch actually takes: seed `templates/basic`", "after": "Exercises the shipped template through the path a first launch takes: seed `templates/basic`", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/sample.test.ts", "before": "testkit, so this is the only thing that fails when the *committed* sample stops loading —\na broken heading, a scene chunk whose front-matter disagrees with its filename, a `start:`", "after": "testkit, so this is the only test that fails when the committed sample stops loading — from a\nbroken heading, a scene chunk whose front-matter disagrees with its filename, or a `start:`", "labels": ["P10", "P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The three permission doors answer the way an abandoned window does: no plan, no answer, no.", "after": "The three permission doors answer as an abandoned window would: no plan, no answers, no consent", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Bytes an ingest reads as a PNG and not as mock art: the signature, then filler. Ingest looks at\nthe magic number and the placeholder marker and nothing else, so nothing here has to decode.", "after": "Bytes that ingest reads as a PNG rather than as mock art. Ingest checks the magic number and\nthe placeholder marker and nothing else, so these bytes do not have to decode.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "What `workspace.open` refuses on: a session with work in flight is one nobody may replace.", "after": "`workspace.open` refuses to replace a session with work in flight", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The session reads it off `project.yaml`; a build that forgets to is how the runner ends\nup staging portraits nobody asked for.", "after": "The session reads this off `project.yaml`. A build that skips it leaves the runner staging\nportraits nobody asked for.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The Wave 3 write path: `storyGraph` reads the wiring, `editBranches` rewires it. What is\nasserted here is the *decision* — edges, reachability, refusals; which files a patch lands in\nis the chunk-specific describe further down.", "after": "The Wave 3 write path: `storyGraph` reads the wiring, `editBranches` rewires it. These cases\nassert the decision — edges, reachability, refusals. Which files a patch lands in is asserted\nby the chunk-specific describe further down.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "`story.assignLineIds`: the ids reading allocated, written down so an insertion can't move them.\nA fixture chunk is born fully marked — `sceneToFountain` writes what it read — so every test\nhere strips the marks first to get the hand-authored starting point the command exists for.", "after": "`story.assignLineIds` writes down the ids reading allocated, so an insertion cannot move them.\nA fixture chunk arrives fully marked because `sceneToFountain` writes what it read, so every\ntest here strips the marks first to reach the hand-authored starting point the command is for.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The whole point of the plan: with marks written, an insertion allocates a fresh id\ninstead of shifting every id below it onto different prose.", "after": "With marks written, an insertion allocates a fresh id instead of shifting every id after\nit onto different prose.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "spans two scenes spans two files — these assert on *which* files were written, and on the\nfront-matter surviving a patch that only ever meant to touch prose.", "after": "spans two scenes spans two files. These cases assert which files were written, and that the\nfront-matter survives a patch that only meant to touch prose.", "labels": ["P10", "P9"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Drop the `[[line:]]`/`[[nextline:]]` marks `sceneToFountain` wrote, leaving bodies that read\nlike hand-authored ones — a fixture chunk is born fully marked, so nothing else would.", "after": "Drops the `[[line:]]`/`[[nextline:]]` marks `sceneToFountain` wrote, leaving bodies that read\nlike hand-authored ones. A fixture chunk arrives fully marked, so no other case would.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "A comment is the sharp case: re-serializing the YAML would drop it, splicing keeps it.", "after": "Re-serializing the YAML would drop a hand-written comment; splicing keeps it", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The id stays the front-matter's: a body that named itself could contradict its own file.", "after": "The id stays in the front-matter, because a body that named itself could contradict its\nown file", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "`editScene`: the one write path for prose. Every case here is about the *file* — which chunk the\nedit landed in, what it still says, and what stopped existing — because the decision itself is\nalready pinned by `@vn/scriptedit`'s `lineops.test.ts`.", "after": "`editScene` is the one write path for prose. Every case here is about the file: which chunk the\nedit landed in, what it still says, and what stopped existing. The decision itself is already\npinned by `@vn/scriptedit`'s `lineops.test.ts`.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Re-serializing writes the marks: the first prose edit canonicalizes a hand-authored chunk.", "after": "Re-serializing writes the marks, so the first prose edit canonicalizes a hand-authored chunk", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Its id is part of its task hash, so it keeps the one it was minted with; only the file and\nthe covered ids move, which is what keeps the generated image the answer to its own task.", "after": "A shot's id is part of its task hash, so the shot keeps the id it was minted with. Only the\nfile and the covered ids move, which keeps the generated image the answer to its own task.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Saying it is all it does: the storyboard is untouched, because nothing about it changed.", "after": "The edit writes only the scene, leaving the storyboard untouched because the line ids the\nshot covers did not change", "labels": ["P4", "P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "A noop rather than a plain refusal: the control offering it is dropped, not disabled.", "after": "A noop rather than a plain refusal, so the control offering it is dropped rather than\ndisabled", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The timeline's outfit strip is built entirely from `story:coverage`, so what it can offer and\nwhat it says is in force are both this payload. A subject that inherits carries nothing — the\nstrip resolves it, and a map that pre-filled the answer would erase the distinction.", "after": "The timeline's outfit strip is built entirely from `story:coverage`, so this payload decides\nboth what the strip can offer and what it says is in force. A subject that inherits carries\nnothing; the strip resolves it, and pre-filling the answer would erase the distinction.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "`workspace.import` and `story.screenplay`: the migration into the chunk form, and the way back\nout of it. The import is asserted by what the *model* says afterwards — a conversion that lost\nor renamed a scene would detach its shots, so \"same graph\" is the contract, not \"same bytes\".", "after": "`workspace.import` and `story.screenplay` are the migration into the chunk form and the way back\nout of it. The import is asserted by what the model says afterwards. A conversion that lost or\nrenamed a scene would detach its shots, so \"same graph\" is the contract, not \"same bytes\".", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Unimported, the screenplay builds nothing: the graph is empty and says why.", "after": "Before the import the screenplay builds nothing, so the graph is empty and says why", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The graph the import produced is the graph the same story authored as chunks gives.", "after": "The import produces the same graph as the same story authored as chunks", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Same refusal from the other side: chunks removed, there is no screenplay either.", "after": "With the chunks removed there is no screenplay either, which refuses from the other side", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Clean output is a reading copy: the ids and the branches go with the markers.", "after": "Clean output is a reading copy, so the ids and the branches go with the markers", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The same bytes `create_skill` writes: a reader cannot tell which made the file.", "after": "The same bytes `create_skill` writes, so a reader cannot tell which path made the file", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "And it is a skill the agent can find, not a file nothing reads.", "after": "The scaffolded skill is one the agent can find, not a file nothing reads", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The asymmetry with the agent's `create_skill`, asserted rather than assumed. `writeSkill`\nrefuses an existing **directory**; this goes through `checkDocWrite` and refuses an existing\n**file** — so a directory a human already put a vetted `run.mjs` in takes the author's\nscaffold and rejects the agent's, which is the right way round.", "after": "`writeSkill` refuses an existing directory; this path goes through `checkDocWrite` and refuses\nan existing file. A directory a human put a vetted `run.mjs` in therefore takes the author's\nscaffold and rejects the agent's.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The splice is textual, so the rest of the file is the property worth pinning.", "after": "The splice is textual, so the rest of the file has to survive it", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The count is the whole point of the confirmation: an art style is not art notes on one rung.", "after": "The count is what the confirmation is for, because an art style is not art notes on one rung", "labels": ["P4", "P3"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The whole claim of `story.moveShot` in one assertion: the order of `show` beats changes,\nand both frames still resolve to the images the run already paid for.", "after": "Asserts the whole claim of `story.moveShot`: the order of `show` beats changes, and both\nframes still resolve to the images the run already paid for.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The classroom plate, which every assertion below is about. */", "after": "The classroom plate the following assertions are about. */", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "A precondition is a sentence a surface may show, never a gate the act may lean on: `exec`", "after": "A precondition is only a sentence a surface may show, not a gate the act may lean on. `exec`", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "A portrait in this project may already be accepted — the gate ran. What the refusal has to\nleave alone is whatever that state was.", "after": "A portrait in this project may already be accepted because the gate ran, so the refusal has\nto leave that state as it found it.", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The task that made those bytes is now an orphan: the planner wants a different hash, so", "after": "The task that made those bytes is now an orphan. The planner wants a different hash, so", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "`null` is the only clear: a seed of 0 is one an author chose.", "after": "`null` is the only clear, because a seed of 0 is one an author chose", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "The whole of §15 through the session rather than through `@vn/artgen`: outside bytes come in,\nget attached to a clause, and come off again. An upload is the one reference that pins itself,\nso it is also the case that proves `from` is optional the whole way down.", "after": "Covers §15 through the session rather than through `@vn/artgen`: outside bytes come in, get\nattached to a clause, and come off again. An upload is the one reference that pins itself, so\nit also proves `from` is optional the whole way down.", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Nothing generated it, so there is no work to bless — the mirror of the concept refusal.", "after": "Nothing generated the upload, so there is no work to bless; the concept refusal mirrors this", "labels": ["P4"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "A prefix addresses it, the way an author reads one off the screen.", "after": "A prefix addresses the reference, the way an author reads one off the screen", "labels": ["P6"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "A picture the pipeline never asked for is never waiting on anyone, so a concept is never here.", "after": "A concept is left out, because the pipeline never asked for it and nothing waits on it", "labels": ["P1"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "Its own project, because replacing a plate writes a second `done` for that plate's task and the\nassertions about *which* bytes are in the slot only mean anything on a run nothing else moved.", "after": "These cases build their own project, because replacing a plate writes a second `done` for that\nplate's task and the assertions about which bytes are in the slot only hold on an untouched run.", "labels": ["P10"]}
{"repo": "visualnovel", "commit": "8d5b59bf", "file": "apps/desktop/src/main/tests/session.test.ts", "before": "One act: the bytes came in as a reference and left as the plate.", "after": "In one act the bytes came in as a reference and left as the plate", "labels": ["P4"]}
